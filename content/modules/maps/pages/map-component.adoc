= Компонент карты

UI-компонент `GeoMap` отображает географические данные, предоставляемые xref:maps:layers-sources.adoc[слоями].

Карта строится путем наложения слоев. Изначально карта не имеет слоев.

Вы можете определить общие параметры карты вместе со слоями в XML-дескрипторе экрана.

[[map]]
== Карта

Чтобы добавить компонент на экран, используйте Jmix Studio.

Нажмите *Add Component* на панели действий, найдите элемент `GeoMap` и дважды щелкните по нему.

image::adding-map.png[align="center", width="1136"]

Новый элемент `geoMap` будет добавлен как на панели структуры *Jmix UI*, так и в XML. Вы можете настраивать атрибуты, такие как xref:flow-ui:vc/common-attributes.adoc#id[id], xref:flow-ui:vc/common-attributes.adoc#height[height], xref:flow-ui:vc/common-attributes.adoc#width[width] и т.д., так же, как это делается для других xref:flow-ui:vc/components.adoc[UI-компонентов].

[source,xml,indent=0]
----
<maps:geoMap id="map"
             height="100%"
             width="100%"/>
----

Если вы не используете дизайнер экранов, объявите пространство имен `maps` в XML-дескрипторе вашего экрана вручную:

[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=xmlns]
----

[[how-to-inject]]
Вы можете инжектировать UI-компонент или слой карты в контроллер, используя действие *Inject to Controller* на панели структуры *Jmix UI*:

image::inject-map-from-hierarchy.png[align="center", width="368"]

Альтернативно, вы можете использовать кнопку *Inject*, доступную на панели действий:

image::inject-map.png[align="center", width="682"]

NOTE: Чтобы компонент можно было инжектировать в контроллер, у него должен быть указан атрибут `id`.

Теперь вы можете взаимодействовать с компонентом `GeoMap` программно, обращаясь к его методам напрямую:

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=geoMap;onInit]
----

[[layer]]
== Слой

Компонент `GeoMap` может включать несколько xref:maps:layers-sources.adoc[слоев], которые отображают различные типы географической информации, такие как растровые слои и векторные слои.

Изначально карта не имеет слоев.

Например, давайте добавим xref:maps:layers-sources.adoc#tile-layer[TileLayer] на карту.

Выберите элемент `geoMap` на панели структуры *Jmix UI* или в XML-дескрипторе экрана, затем нажмите кнопку *Add* на панели инспектора. В выпадающем списке выберите *Layers -> TileLayer*.

image::add-tile-layer.png[align="center", width="331"]

Вы можете <<how-to-inject,инжектировать>> слой в контроллер и взаимодействовать с ним программно, обращаясь к его методам напрямую:

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=mapTile;onInit]
----

[[source]]
== Источник

xref:maps:layers-sources.adoc[Источник] представляет данные, которые используются для отрисовки слоев карты на компоненте карты. Источники предоставляют пространственную информацию, которая определяет содержимое и внешний вид слоев карты.

Например, давайте добавим xref:maps:layers-sources.adoc#osm-source[OsmSource] для тайлового слоя.

Выберите `maps:tile` на панели структуры *Jmix UI* или в XML-дескрипторе экрана, а затем нажмите кнопку *Add* на панели инспектора. В выпадающем списке выберите *OsmSource*.

image::add-osmsource.png[align="center", width="898"]

[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=osmSource]
----

Вы можете <<how-to-inject,инжектировать>> источник в контроллер и взаимодействовать с ним программно, обращаясь к его методам напрямую:

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=osmSource;onInit]
----
// You can specify additional parameters:
//
// * `attributions` - information about copyrights and licences that need to be displayed on the map when using data from that source. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_Source-Source.html[Source^].
// * `cacheSize` - the tile cacheSize. Note, the `cacheSize` attribute is set only at creation time and cannot be changed at runtime. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_Tile-TileSource.html[TileSource^].
// * `crossOrigin` is used for loaded images. Note, `crossOrigin` is set only at creation time and cannot be changed at runtime. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_OSM-OSM.html[OSM^].
// * `maxZoom` sets the maximum zoom level. The default value is 42. Note, the `maxZoom` attribute is set only at creation time and cannot be changed at runtime. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_XYZ-XYZ.html[XYZ^].
// * `opaque` sets whether the layer is opaque. The default value is `true`. Note, the `opaque` attribute is set only at creation time and cannot be changed at runtime. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_OSM-OSM.html[OSM].
// * `url` sets the URL template. Must include \{x}, \{y} or {-y}, and \{z} placeholders. The default value is `++https://tile.openstreetmap.org/{z}/{x}/{y}.png++`. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_OSM-OSM.html[OSM^].
// * `wrapX` sets whether the source should be looped along the X coordinate. The default value for `Source` is `false`. Note, the `wrapX` attribute is set only at creation time and cannot be changed at runtime. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_source_Source-Source.html[Source^].

[[view]]
== Отображение

https://openlayers.org/en/v8.2.0/apidoc/module-ol_View-View.html[Отображение (View)^] определяет, как карта отображается с точки зрения ее центра, уровня масштабирования, поворота и проекции. По сути, отображение задает начальное состояние карты при ее загрузке в пользовательском интерфейсе.

По умолчанию компонент `geoMap` отображает карту мира с начальным географическим центром в `(0,0)`.

Выберите `geoMap` на панели структуры *Jmix UI* или в XML-дескрипторе экрана, а затем нажмите кнопку *Add* на панели инспектора. В выпадающем списке выберите *MapView*.

image::add-map-view.png[align="center", width="372"]

[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=mapView]
----

Вы можете указать дополнительные параметры:

* `centerX` определяет широту начального географического центра карты. Передается в объект `org.locationtech.jts.geom.Coordinate`.
* `centerY` определяет долготу начального географического центра карты. Передается в объект `org.locationtech.jts.geom.Coordinate`.
* `maxZoom` - устанавливает максимальный уровень масштабирования для отображения.
//The default value is 28. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_View-View.html#setMaxZoom[setMaxZoom^].
* `minZoom` - устанавливает минимальный уровень масштабирования для отображения.
//The default value is 0. For more details see https://openlayers.org/en/v8.1.0/apidoc/module-ol_View-View.html#setMinZoom[setMinZoom^].
* `projection` определяет систему координат (CRS), используемую для отображения географических данных на карте. Она преобразует трехмерные координаты Земли в двухмерную плоскость. Распространенные проекции включают `EPSG:3857` (Web Mercator) и `EPSG:4326` (WGS 84). Проекция по умолчанию - https://epsg.io/3857[EPSG:3857^]. Вы можете задать пользовательскую проекцию:
+
[[set-custom-projection]]
[TIP]
====
++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.ru/ui-samples/sample/geo-map-custom-projection" class="live-demo-btn" target="_blank">ДЕМО</a>
</div>
++++

*Настройка пользовательской проекции*

Выполните следующие шаги для настройки пользовательской проекции:

. *Определите бин проекции*. Зарегистрируйте проекцию в вашей Spring-конфигурации, используя формат https://epsg.io/21781.proj4[PROJ.4^]:
+
[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/MapsEx2Application.java[tags=register-projection]
----

. *Настройте компонент карты*. Укажите атрибут `projection` для `mapView` в вашем XML-дескрипторе:
+
[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/customprojection/custom-projection-view.xml[tags=map]
----

. *Инициализируйте в контроллере*. Примените проекцию программно ко всем компонентам карты на странице:
+
[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/customprojection/CustomProjectionView.java[tags=set-projection]
----
+
NOTE: Проекция применяется ко всем компонентам карты на странице. После установки все операции с координатами используют единицы измерения проекции (метры, градусы и т.д.).
====
* `rotation` - устанавливает поворот для отображения в радианах (положительный поворот по часовой стрелке, 0 означает Север). Подробнее смотрите https://openlayers.org/en/v8.2.0/apidoc/module-ol_View-View.html#setRotation[setRotation^].
* `zoom` определяет конкретный уровень масштабирования. Уровень масштабирования начинается с 0, представляя вид с максимальным отдалением, и постепенно увеличивается по мере приближения.

[[extent]]
== Экстент

https://openlayers.org/en/v8.2.0/apidoc/module-ol_extent.html#~Extent[Экстент (extent)^] задает географические границы <<view,отображения>> карты или конкретного <<layer,слоя>> в терминах координат, определяя видимую область на карте.

Экстент обычно определяется минимальными (левый верхний угол) и максимальными (правый нижний угол) значениями координат в форме [`minX`, `minY`, `maxX`, `maxY`], представляя ограничивающую рамку области.

Выберите слой или элемент `mapView` на панели структуры *Jmix UI* или в XML-дескрипторе экрана, а затем нажмите кнопку *Add* на панели инспектора. В выпадающем списке выберите *Extent* и настройте атрибуты `minX`, `minY`, `maxX` и `maxY`, как указано ниже.

[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=extent]
----

[[extent-constraint-options]]
=== Параметры ограничения экстента

По умолчанию, когда для `mapView` задан `extent`, OpenLayers ограничивает всю область просмотра, чтобы она оставалась внутри этого экстента. В адаптивных макетах это может привести к принудительному масштабированию карты, если соотношение сторон контейнера карты отличается от соотношения сторон экстента.

Для настройки этого поведения используйте следующие атрибуты `mapView`:

* `showFullExtent` - позволяет отмасштабироваться настолько, чтобы отобразить весь настроенный экстент, даже если соотношение сторон контейнера отличается от него. Значение по умолчанию: `false`.
* `constrainOnlyCenter` - применяет ограничение экстента только к центру области просмотра, поэтому края области просмотра могут выходить за пределы экстента. Значение по умолчанию: `false`.
* `smoothExtentConstraint` - применяет ограничение экстента плавно, позволяя небольшой перескок с эффектом возврата вместо жесткой остановки. Значение по умолчанию: `true`.

Эти атрибуты являются параметрами времени создания. Они передаются в представление OpenLayers во время инициализации и не могут быть изменены во время выполнения.

[source,xml,indent=0]
----
include::example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapcomponent/map-component-view.xml[tags=extentConstraintOptions]
----