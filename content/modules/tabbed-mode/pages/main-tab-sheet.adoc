= MainTabSheet Component

A container that is intended to display open views in tabs. Only the content of the selected tab is visible at a time.

[cols="1h,6", grid=none]
|===
|XML Element
|`mainTabSheet`
|Java Class
|`MainTabSheet`
|Attributes
|xref:flow-ui:vc/common-attributes.adoc#id[id] -
xref:flow-ui:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:flow-ui:vc/common-attributes.adoc#classNames[classNames] -
xref:flow-ui:vc/common-attributes.adoc#colspan[colspan] -
xref:flow-ui:vc/common-attributes.adoc#css[css] -
xref:flow-ui:vc/common-attributes.adoc#height[height] -
xref:flow-ui:vc/common-attributes.adoc#width[width] -
xref:flow-ui:vc/common-attributes.adoc#maxHeight[maxHeight] -
xref:flow-ui:vc/common-attributes.adoc#maxWidth[maxWidth] -
xref:flow-ui:vc/common-attributes.adoc#minHeight[minHeight] -
xref:flow-ui:vc/common-attributes.adoc#minWidth[minWidth] -
xref:flow-ui:vc/common-attributes.adoc#visible[visible] -
<<theme-variants, themeNames>> -
<<drag-and-drop, tabsDraggable>>
|Handlers
|xref:flow-ui:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:flow-ui:vc/common-handlers.adoc#DetachEvent[DetachEvent] -
<<selected-change-event, SelectedChangeEvent>> -
<<tabs-collection-change-event, TabsCollectionChangeEvent>>
|Elements
|<<actions,actions>>
|===

[[Basics]]
== Basics

To create the component, use the `mainTabSheet` XML element inside the `workArea` element. Both `workArea` and `mainTabSheet` are available only in the main view descriptor.

The following example demonstrates how a basic `mainTabSheet` is created:

[source,xml,indent=0]
----
include::example$/tabbedmode-ex1/src/main/resources/com/company/demo/view/main/main-view.xml[tags=mainTabSheet]
----

[[actions]]
== Actions

The `mainTabSheet` component implements the `HasActions` interface and can contain both standard and custom actions. Actions are invoked from the context menu that appears on the right click on the tabs area.

image::main-tab-sheet/tabsheet-context-menu.png[align="center", width="500"]

The standard actions are the following:

[[tabmod_closeThisTab]]
=== tabmod_closeThisTab

Action class: `CloseThisTabAction`

Closes the selected tab.

[[tabmod_closeOtherTabs]]
=== tabmod_closeOtherTabs

Action class: `CloseOtherTabsAction`

Closes all currently open tabs in the `mainTabSheet` except the selected tab.

[[tabmod_closeAllTabs]]
=== tabmod_closeAllTabs

Action class: `CloseAllTabsAction`

Closes all currently open tabs in the `mainTabSheet`.

[[drag-and-drop]]
== Drag and Drop

`mainTabSheet` supports dragging and dropping tabs. This feature can be used, for example, to rearrange tabs.

By changing the `tabsDraggable` attribute value, you can set whether the tabs are draggable by the user. Default value is `false`.

// image::main-tab-sheet/tabs-reordering.gif[align="center"]

[[handlers]]
== Events and Handlers

[[selected-change-event]]
=== SelectedChangeEvent

Whenever the user switches between tabs, the `io.jmix.tabbedmode.component.workarea.TabbedViewsContainer.SelectedChangeEvent` is fired. Create an event handler to execute custom code in response to the tab switch.

The following code will send a notification on every tab switch:

[source,java,indent=0]
----
include::example$/tabbedmode-ex1/src/main/java/com/company/demo/view/main/MainView.java[tags=notifications;selectedChangeEvent]
----

TIP: To generate handler method stubs in the Studio, use the *Generate Handler* button in the top panel of the screen controller code. Or you can select the component in the screen descriptor layout and use the *Handlers* tab of the *Component Inspector* panel.

[[tabs-collection-change-event]]
=== TabsCollectionChangeEvent

Whenever the user opens or closes views, which leads to the creation of a new tab or the removal of an existing one, the `io.jmix.tabbedmode.component.workarea.TabbedViewsContainer.TabsCollectionChangeEvent` is fired. Create an event handler to execute custom code in response to the tabs collection change.

The following code will send a notification on every tabs collection change:

[source,java,indent=0]
----
include::example$/tabbedmode-ex1/src/main/java/com/company/demo/view/main/MainView.java[tags=notifications;tabsCollectionChangeEvent]
----

TIP: To generate handler method stubs in the Studio, use the *Generate Handler* button in the top panel of the screen controller code. Or you can select the component in the screen descriptor layout and use the *Handlers* tab of the *Component Inspector* panel.

[[theme-variants]]
== Theme Variants

.`centered`
[%collapsible]
====
Positions the tabs at the center of the main view, instead of their default left-aligned position:

image::main-tab-sheet/tabs-centered.png[align="center", width=700]
====

.`small`
[%collapsible]
====
Makes the tabs smaller:

image:main-tab-sheet/tabs-small.png[align="center", width=500]
====

.`minimal`
[%collapsible]
====
Reduces visual style to only show tab labels without any additional visual elements:

image:main-tab-sheet/tabs-minimal.png[align="center", width=500]
====

.`hide-scroll-buttons`
[%collapsible]
====
Hides the scroll buttons that assist in navigating overflown tabs:

image:main-tab-sheet/tabs-hide-scroll-buttons.png[align="center", width=376]

====

.`bordered`
[%collapsible]
====
Adds a border around the tabs themselves, as well as the content area.

image:main-tab-sheet/tabsheet-bordered.png[align="center", width=832]
====

== Tab Text Max Width

You can limit the width of the main tab text by setting the value of the CSS variable in the application theme:

[source,css,indent=0]
----
include::example$/tabbedmode-ex1/src/main/frontend/themes/tabbedmode-ex1/tabbedmode-ex1.css[tags=tab-text-max-width]
----

image:main-tab-sheet/tab-text-max-width.png[align="center", width=400]