= REST 配置

Jmix REST 应用程序属性支持配置通用 REST 不同方面的行为。

[[entities-api-properties]]
== 实体 API


[[jmix.rest.default-max-fetch-size]]
=== jmix.rest.default-max-fetch-size

定义 API 返回实体实例数量的默认最大值。

默认值：`10000`


[[jmix.rest.entity-max-fetch-size]]
=== jmix.rest.entity-max-fetch-size

定义 API 返回特定实体实例数量的默认最大值。

下面示例中，为两个实体配置了 `entityMaxFetchSize`。所有其他实体则会使用 <<jmix.rest.default-max-fetch-size, jmix.rest.default-max-fetch-size>> 配置的值。

[source,properties]
----
jmix.rest.entity-max-fetch-size.sample_Product = 500
jmix.rest.entity-max-fetch-size.sample_Customer = 1000
----

[[jmix.rest.inline-enabled-file-extensions]]
=== jmix.rest.inline-enabled-file-extensions

可以通过 `Content-Disposition: inline` 请求头 xref:files-api.adoc#downloading-files[下载] 的文件类型扩展名。

默认值：`jpg,png,jpeg,pdf`

[[jmix.rest.inline-fetch-plan-enabled]]
=== jmix.rest.inline-fetch-plan-enabled

如果设置为 `false`，则所有 REST 端点都会拒绝 xref:rest:entities-api/load-entities.adoc#inline-fetch-plans[内联 Fetch Plans]

默认值：`true`

[[jmix.rest.json-transformation-config]]
=== jmix.rest.json-transformation-config

定义包含一组 JSON 转换配置的文件，由 REST API 在客户端需要某些特定版本格式的数据时使用。

通过 Resources 接口加载，因此文件可位于 classpath 或者配置文件夹中。

// TODO: [MD] link to configuration directory description once it is available in the docs

默认值：`none`

[[jmix.rest.max-upload-size]]
=== jmix.rest.max-upload-size

定义 REST API 支持上传的文件大小的最大值（单位为字节）。

文件通过 `Resources` 接口加载，因此文件可位于 classpath 或者配置文件夹中。

默认值：`20971520`（20 MB）

[[jmix.rest.optimistic-locking-enabled]]
=== jmix.rest.optimistic-locking-enabled

如果 JSON 中提供了 `version` 属性，则启用实体乐观锁。

默认值：`false`

[[jmix.rest.queries-config]]
=== jmix.rest.queries-config

定义包含 JPQL 查询语句的文件，JPQL 查询语句用于 xref:rest:entities-api/load-entities.adoc#load-list-jpql[实体 API]。

[source,properties]
----
jmix.rest.queries-config = rest/sample/rest-queries.xml
----

通过 Resources 接口加载，因此文件可位于 classpath 或者配置文件夹中。

// TODO: [MD] link to configuration directory description once it is available in the docs

默认值：`none`

[[jmix.rest.services-config]]
=== jmix.rest.services-config

定义包含一组服务方法的文件，这些服务方法开放为 xref:rest:business-logic.adoc#services-api[服务 API]。

[source,properties]
----
jmix.rest.services-config = rest/sample/rest-services.xml
----

通过 Resources 接口加载，因此文件可位于 classpath 或者配置文件夹中。

// TODO: [MD] link to configuration directory description once it is available in the docs

默认值：`none`


[[jmix.rest.response-fetch-plan-enabled]]
=== jmix.rest.response-fetch-plan-enabled

如果设置为 `true`，则可以在创建和更新请求中使用 `responseFetchPlan` 参数指定 fetch plan 名称。此时，创建/更新后的实体使用参数指定的 fetch plan 加载后以 JSON 格式返回。

如果设置为 `false`，返回体仅包含创建/更新后实体的最简单内容。

默认值：`true`

[[paths]]
== 路径

[[jmix.rest.base-path]]
=== jmix.rest.base-path

通用 REST 端点的基本路径。

默认值：`/rest`

[[jmix.rest.entities-path]]
=== jmix.rest.entities-path

xref:entities-api.adoc[] 端点的路径。

默认值：`/entities`

[[jmix.rest.docs-path]]
=== jmix.rest.docs-path

xref:api-docs.adoc[] 端点的路径。

默认值：`/docs`

[[jmix.rest.metadata-path]]
=== jmix.rest.metadata-path

xref:metadata-api.adoc[] 端点的路径。

默认值：`/metadata`

[[jmix.rest.files-path]]
=== jmix.rest.files-path

xref:files-api.adoc[] 端点的路径。

默认值：`/files`

[[jmix.rest.messages-path]]
=== jmix.rest.messages-path

xref:messages-api.adoc[] 端点的路径。

默认值：`/messages`

[[jmix.rest.permissions-path]]
=== jmix.rest.permissions-path

xref:user-session-api.adoc#permissions[权限] 端点的路径。

默认值：`/permissions`

[[jmix.rest.queries-path]]
=== jmix.rest.queries-path

xref:entities-api/load-entities.adoc#load-list-jpql[使用 JPQL 加载实体] 端点的路径。

默认值：`/queries`

[[jmix.rest.services-path]]
=== jmix.rest.services-path

xref:business-logic.adoc#services-api[服务 API] 端点的路径。

默认值：`/services`

[[jmix.rest.user-info-path]]
=== jmix.rest.user-info-path

xref:user-session-api.adoc#user-info[用户信息] 端点的路径。

默认值：`/userInfo`

[[jmix.rest.capabilities]]
=== jmix.rest.capabilities

xref:capabilities-api.adoc[] 端点的路径。

默认值：`/capabilities`

[[security-properties]]
== 安全

参阅 xref:security:custom-endpoints.adoc#application-properties[安全：自定义端点] 部分了解如何为 REST 端点配置应用程序属性。
