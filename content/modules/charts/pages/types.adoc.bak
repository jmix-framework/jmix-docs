= Charts Types
:figure-caption!:

There are several chart types supported by Jmix framework.

.Charts types hierarchy
image::charts/charts-hierarchy-diagram.png[align="center"]

This section describes the implementation of the chart interfaces. All these components have the `NAME` constant, so they can be created using `UiComponents`.
