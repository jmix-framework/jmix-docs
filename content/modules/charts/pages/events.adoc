= 事件

对每种类型的图表，都可以配置不同图表事件的处理方法。

include::ui:page$vcl/events-hanlers-generation-tip.adoc[]

在 xref:charts:types.adoc[图表类型] 章节相应部分列举了每种图表支持处理的事件列表。

事件处理的示例在 xref:charts:examples.adoc#using-events[使用事件] 章节。

[[common-events]]
== 通用事件

所有类型的图表都可以处理下列类型的事件：

* `LegendItemHideEvent` - 当隐藏图例条目时发送。
* `LegendItemShowEvent` - 当展示图例条目时发送。
* `LegendLabelClickEvent` - 当点击图例标签时发送。
* `LegendMarkerClickEvent` - 当点击图例标记时发送。

除了以上事件之外，`SeriesBasedChart` 接口包含 `zoomOut` 、`zoomToIndexes` 和 `zoomToDates` 方法用来操纵图表的轴。

类似的 ，`CoordinateChart` 接口也提供这些方法用来操纵值坐标轴：`zoomOutValueAxes`、`zoomOutValueAxis`、`zoomOutValueAxis`、`zoomValueAxisToValues` 和 `zoomValueAxisToValues`。

[[other-events]]
== 其他事件

[[axis-zoom-event]]
=== AxisZoomEvent

此事件当图表的轴缩放时发送。

[[category-item-click-event]]
=== CategoryItemClickEvent

此事件当点击类目轴的一个类目时发送。

[[cursor-period-select-event]]
=== CursorPeriodSelectEvent

此事件当用光标选择展示的时段时发送。

[[cursor-zoom-event]]
=== CursorZoomEvent 

此事件当用鼠标缩放图表区域时发送。

[[chart-click-event]]
=== ChartClickEvent

此事件当用户点击画布时发送。

[[chart-right-click-event]]
=== ChartRightClickEvent

此事件当用户右键点击画布时发送。

[[graph-click-event]]
=== GraphClickEvent 

此事件当点击图表时发送。

[[graph-item-click-event]]
=== GraphItemClickEvent 

此事件当点击图表中条目时发送。

[[graph-item-right-click-event]]
=== GraphItemRightClickEvent 

此事件当右键点击图表条目时发送。

[[slice-click-event]]
=== SliceClickEvent

此事件当点击饼图中的一块时发送。

[[slice-pull-in-event]]
=== SlicePullInEvent

此事件当饼图的一块移入图表时发送。

[[slice-pull-out-event]]
=== SlicePullOutEvent

此事件当饼图的一块移出图表时发送。

[[slice-right-click-event]]
=== SliceRightClickEvent

此事件当右键点击饼图的一块时发送。

[[roll-out-graph-event]]
=== RollOutGraphEvent 

此事件当用户鼠标移出图表时发送。

[[roll-out-graph-item-event]]
=== RollOutGraphItemEvent 

此事件当用户鼠标移出数据项时发送。

[[roll-over-graph-event]]
=== RollOverGraphEvent 

此事件当用户鼠标悬停在图表时发送。

[[roll-over-graph-item-event]]
=== RollOverGraphItemEvent 

此事件当用户鼠标悬停在数据项时发送。

[[zoom-event]]
=== ZoomEvent 

此事件当缩放画布时发送。

