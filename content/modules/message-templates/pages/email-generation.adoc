= 3. Генерация email из шаблонов

Давайте рассмотрим пример генерации электронного письма на основе шаблонов темы и тела сообщения, созданных в предыдущем разделе.

Прежде чем мы начнем, убедитесь, что установлено дополнение xref:email:index.adoc[Email Sending]. Если оно еще не установлено, пожалуйста, установите его перед продолжением.

Далее настройте необходимые xref:email:configuration.adoc[параметры отправки] электронной почты, чтобы обеспечить успешную доставку писем.

[[adding-button]]
== Добавление кнопки об уведомлении

Добавьте кнопку *Notify by Email* в экран списка бронирований. Она позволит отправлять уведомление по электронной почте создателю выбранного бронирования.

Добавьте новое действие `notifyEmail` в `booking-list-view.xml` внутри `<actions>`:

[source,xml,indent=0]
----
include::../examples/messagetemplates-ex1/src/main/resources/com/company/sample/view/booking/booking-list-view.xml[tags=notify-email]
----

Затем свяжите это действие с кнопкой в контейнере `buttonsPanel`:

[source,xml,indent=0]
----
include::../examples/messagetemplates-ex1/src/main/resources/com/company/sample/view/booking/booking-list-view.xml[tags=buttons-panel;notify]
----

[[generate-email]]
== Генерация email на основе шаблона

Сгенерируйте метод-обработчик события `ActionPerformedEvent` для действия `notifyEmailButton`. Добавьте в него следующую логику:

[source,java,indent=0]
----
include::../examples/messagetemplates-ex1/src/main/java/com/company/sample/view/booking/BookingListView.java[tags=booking-list-view]
----
<1> Бин для генерации сообщений из шаблонов.
<2> Бин для отправки электронных писем.
<3> Бин для отображения уведомлений пользователю.
<4> Извлекает выбранное бронирование (`booking`) и его создателя (`creator`) из `bookingsDataGrid`.
<5> Проверяет, указан ли адрес электронной почты у создателя (`creator`) бронирования. Если нет, отображается уведомление, и метод завершает работу.
<6> Генерирует тему и тело сообщения, используя шаблоны FreeMarker (`booking-email-subject` и `booking-email-body`).
<7> Передает динамические параметры (`booking`, `today`) в шаблоны.
<8> Создает объект `EmailInfo`, содержащий адрес электронной почты получателя, тему письма, тело письма и тип контента.
<9> Пытается отправить электронное письмо, используя бин `emailer`. В случае ошибки отображается уведомление о неудаче.
<10> Отображает уведомление об успехе, если электронное письмо успешно отправлено.

[[sending-viewing-email]]
== Отправка и просмотр электронных писем

Давайте проверим функциональность процесса генерации электронных писем на основе шаблонов. Эта проверка необходима, чтобы убедиться, что данные о бронировании корректно подставляются в шаблон сообщения и доставляются адресату.

. Запустите приложение и перейдите в экран *Application -> Bookings*.

. Создайте новую запись о бронировании, выберите пользователя с действующим адресом электронной почты и заполните остальные поля.

. В таблице бронирований выберите созданную запись и нажмите *Notify by Email*.

. Приложение должно показать уведомление об успешной отправке.

image::resulting-email.png[align="center", width="1132"]

[[summary]]
== Резюме

В этом разделе вы узнали, как работать с шаблонами сообщений для автоматизации отправки уведомлений по электронной почте в вашем приложении.

Вы получили практический опыт добавления кнопки *Notify by Email* в экран, настройки действия для запуска процесса отправки электронной почты и написания кода для динамической генерации электронного сообщения с использованием данных из выбранной сущности.
Вы также узнали о важности установки дополнения Email Sending и настройки параметров отправки электронной почты.
