= 组合角色

多个角色可以基于以下规则组合创建角色层次结构：

* 资源角色只能与其他资源角色组合。
* 行级角色只能与其他行级角色组合。
* 如果某个角色被分配给用户，则其所有父角色也会生效。

角色可以在设计时组合 — 通过编写代码或使用 xref:studio:role-designer.adoc#resource-role-designer[角色设计器]。在运行时，可以通过安全视图管理角色层次结构。

== 设计时组合资源角色

代码中的角色组合类似于一个接口扩展另一个接口。例如，资源角色 `SystemOwner` 可以从多个限制性更强的角色获取策略，然后添加自己的附加策略来获得更多的访问权限。

[source,java,indent=0]
----
include::example$/security-ex1/src/main/java/com/company/demo/security/SystemOwnerRole.java[tags=combined-role]
----

使用 Studio 的 xref:studio:role-designer.adoc#resource-role-designer[角色设计器] 支持可视化组合角色，能实时看到组合后的策略结果。

image:resource-role-designer.png[width = 1016]

== 运行时组合资源角色

运行时，可以通过 *Security -> Resource roles* 视图组合资源角色。创建或选择一个资源角色进行编辑，然后可以添加一个或多个 *Base roles（基础角色）*。

image:combined-runtime-resource-role.png[width=1055]

最终的权限集是角色自身的权限加上所选基础角色授予的所有权限。

NOTE: 基础角色只能添加在运行时创建的角色中。

== 设计时组合行级角色

行级角色的组合也是通过接口扩展的方式完成。例如，`SameRegionRole` 组合了几个角色，这些角色可以让用户仅能看到自己区域的数据。

[source,java,indent=0]
----
include::example$/security-ex1/src/main/java/com/company/demo/security/SameRegionRole.java[tags=combined-role]
----

== 运行时组合行级角色

运行时，可以通过 *Security -> Row-level roles* 视图组合行级角色。创建或选择一个行级角色进行编辑，然后可以添加一个或多个 *Base roles（基础角色）*。

image:combined-runtime-row-level-role.png[width=1088]

最终的权限集是角色自身的权限加上所选基础角色授予的所有权限。

NOTE: 基础角色只能添加在运行时创建的角色中。
