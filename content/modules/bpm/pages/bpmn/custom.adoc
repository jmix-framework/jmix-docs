= 自定义表单

当某个元素需要的表单无法以简单的 xref:bpm:input-dialog.adoc[] 显示，也无法使用常规的带注解的 xref:bpm:bpmn/jmix-view-forms.adoc[] 方式时，可以提供一个自定义表单。该表单仍然可以是 Jmix 视图，也可以使用其他 UI 技术（例如 React）构建。

在建模器中，需要为此表单提供一个 id。自定义表单必须读取提供的输入参数并返回结果。

image::custom-forms/custom-form.png[align="center", width="861"]

== 自定义 Jmix 表单

如需在 *启动流程* 或 *我的任务* 视图中渲染自定义表单，请实现 `ProcessFormViewCreator` 接口，并将其注册到 `custom` 表单类型。

我们来看一个实现 `ProcessFormViewCreator` 的 `MyCustomProcessFormViewCreator` 示例：

[source,java,indent=0]
----
include::example$bpm-ex1/src/main/java/com/company/bpmex1/app/MyCustomProcessFormViewCreator.java[tags=custom]
----

<1> `@Order` 注解使该实现成为框架的首选。
<2> 重写用于创建启动流程表单的方法。
<3> 检查视图是否实现了 `AcceptsProcessDefinition`。此时，需要为视图设置 `ProcessDefiniton`。
<4> 重写用于创建任务流程表单的方法。
<5> 检查视图是否实现了 `AcceptsTask`。此时，需要为视图设置 `Task`。

上面的代码使用了两个辅助接口。辅助接口 `AcceptsProcessDefinition` 的示例：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/java/com/company/bpmex1/view/forms/AcceptsProcessDefinition.java[tags=custom]
----
辅助接口 `AcceptsTask`：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/java/com/company/bpmex1/view/forms/AcceptsTask.java[tags=custom]
----

这些接口必须由自定义的启动和任务表单实现。

=== 自定义启动表单

示例中的自定义启动表单仅带有一个 *Start process* 按钮：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/resources/com/company/bpmex1/view/forms/custom-start-form.xml[tags=custom]
----

自定义启动表单的视图控制器：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/java/com/company/bpmex1/view/forms/CustomStartForm.java[tags=custom]
----

=== 自定义任务表单

示例中的自定义任务表单仅带有一个 *Task complete* 按钮：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/resources/com/company/bpmex1/view/forms/custom-task-form.xml[tags=custom]
----

自定义任务表单的视图控制器：

[source,java,indent=0]
----
include::example$/bpm-ex1/src/main/java/com/company/bpmex1/view/forms/CustomTaskForm.java[tags=custom]
----

== 使用其他的 UI 技术

可以使用其他 UI 技术（例如 React）创建表单，通过 REST 与 Jmix BPM 通信。使用 Flowable 的 `FormService` 可以从引擎获取表单信息，该服务提供以下方法：

* `StartFormData getStartFormData (String processDefinitionId)`
* `TaskFormData getTaskFormData (String taskId)`

或者，使用 Jmix 中的 `ProcessFormDataExtractor` 暴露表单元数据。示例请参见 xref:process-form-data-extractor.adoc[] 部分。
