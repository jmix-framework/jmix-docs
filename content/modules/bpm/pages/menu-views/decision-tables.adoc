= 决策表

*Decision Tables（决策表）* 视图显示已部署决策表的列表，支持在建模器中打开、查看详细信息或从 XML 文件上传。

NOTE: 默认情况下，列表是空的。点击 *Apply* 加载所有数据。要查看特定决策表，设置筛选条件后再次点击 *Apply*。

image::bpm-menu-views/decision-tables.png[,800]

打开 *Decision editor* 视图后，可以检查决策表的 XML 定义。同时，也可以移除部署。

image::bpm-menu-views/decision-table-detail.png[,800]
