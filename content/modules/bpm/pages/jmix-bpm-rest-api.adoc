= Flowable REST API

集成了 BPM 扩展组件的 Jmix 应用程序可以使用 Flowable 的 REST API：

* *BPMN REST API* 端点，支持处理流程定义、流程实例的运行时和历史、用户任务以及其他（参考 https://www.flowable.com/open-source/docs/bpmn/ch14-REST[Flowable 文档^]）。

* *DMN REST API* 端点，支持管理决策表（参考 https://www.flowable.com/open-source/docs/dmn/ch07-REST[Flowable 文档^]）。

[[gradle-dependencies]]
== Gradle 依赖

如需使用 API，需要添加一些特定的依赖：

.build.gradle
[source]
----
include::example$/bpm-ex1/build.gradle[tags=dependencies;flowable-bpmn-api-starter;flowable-dmn-api-starter]
----

[[base-path-configuration]]
== 根路径配置

REST API 可以通过不同的根路径访问。默认为：

[cols="1h,1,4", grid=rows]
|===
| |根路径 |示例 URL

|BPMN REST API
|`/process-api`
|`\http://localhost:8080/process-api/repository/process-definitions`

|DMN REST API
|`/dmn-api`
|`\http://localhost:8080/dmn-api/dmn-repository/deployments`
|===

[NOTE]
====
如果设置了上下文路径，则需要添加在 URL 中。
例如，使用 `server.servlet.context-path=/app` 配置时，DMN 的 URL 就变成：
`\http://localhost:8080/app/rest/bpm/dmn/dmn-repository/deployments`
====

如需修改默认的根路径，可以在 `application.properties` 中配置：

.application.properties
[source,properties]
----
include::example$/bpm-ex1/src/main/resources/application.properties[tags=process-path;dmn-path]
----

[[security-configuration]]
== 安全配置

添加依赖后，在 Jmix 项目中为 Flowable REST 端点配置Spring Security。xref:security:custom-endpoints.adoc[] 部分提供了以下场景的示例代码：

* 开放的端点（无需认证）
* HTTP 基本认证
* 基于 Token 的认证