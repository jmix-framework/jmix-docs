= Default Icons

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/icon-all" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Jmix includes over 600 built-in icons organized into three icon sets:

[cols="1h,6", grid=none, border=none]
|===
|Jmix
|Default icon set. It combines all Vaadin icons, Lumo icons not present in Vaadin, and Jmix-specific icons.
|Vaadin
|Icons provided by the Vaadin framework.
|Lumo
|Icons from the Lumo design system.
|===

[[icon-attribute]]
== Setting Icon via Attribute

Many UI components allow icons to be added through attributes. Some components provide multiple such attributes for different icon slots, such as `icon`, `uploadIcon`, `downloadButtonIcon`, `dropLabelIcon`.

For example:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-1]
----

To use icons from a specific set, add the appropriate prefix:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-2]
----

[TIP]
====
You can browse and select icons in Studio. In the inspector panel, click an icon attribute to open the *Choose Icon* dialog. The dialog displays available icon sets and lets you select an icon visually.

image::icons/choose-icon-dialog.png[align="center", width="592"]
====

[[icon-nested-element]]
== Setting Icon in Nested Element

Some components support adding icons using nested elements that correspond to specific icon slots, such as `<icon>`, `<uploadIcon>`, `<dropdownIcon>`, <prefix>, and <suffix>. This approach allows you to use both built-in and xref:icons/custom-icons.adoc[custom icons], including SVGs, font icons, and images.

For example:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-3]
----

[[programmatic-usage]]
== Setting Icon Programmatically

Icons can be set programmatically. It is recommended to use the `Icons` bean as the central interface for creating UI components that represent icons. This ensures consistent behavior (`Icons` is also used when parsing XML) and simplifies centralized xref:icons/override-icons.adoc[icon overrides].

For example:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/icons/IconsView.java[tags=programmatic-1]
----

Nevertheless, it is also possible to bypass the bean and instantiate icons directly from a specific set:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/icons/IconsView.java[tags=programmatic-2]
----