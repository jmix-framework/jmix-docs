= Adding More Icons

Additional icons can be provided in three ways: as an icon font, as an SVG sprite, or as separate SVG files.

[[icon-fonts]]
== Icon Fonts

An icon font is a collection of icons delivered as a font. Icons are rendered as characters from that font.

TIP: After you add a custom icon font you can use it to override the default application icons. See xref:icons/override-icons.adoc[] for details.

To add an icon font, you will need the following:

* A font file – commonly `.woff` or `.woff2`, or older formats such as `.ttf` or `.eot`.
* A `@font-face` declaration – typically placed in a stylesheet that you import into your theme’s main stylesheet (`styles.css`).
* Optionally, a stylesheet with icon CSS classes — often added alongside the `@font-face` declaration and also imported into `styles.css`.

=== Example: Adding Material Icons

This example shows how to add Material icons to your xref:flow-ui:themes/themes.adoc#application-theme[application theme].

image:flow-ui:icons/material-icons.png[width=298]

Create a directory in the theme folder that contains:

* `material-icons-outlined.woff2`
* `outlined.css` – includes the `@font-face` declaration and the CSS class for icons:
+
.outlined.css
[source,css]
----
include::example$/onboarding/frontend/themes/onboarding/material/outlined.css[tags=material-font-face;material-css]
----

Import the stylesheet into the theme’s main stylesheet:

.styles.css
[source,css]
----
include::example$/onboarding/frontend/themes/onboarding/styles.css[tags=import-material]
----

To create an icon, use `fontIcon` component providing the font family name (matching the `@font-face` declaration), and either a character code or ligature:

[source,xml,indent=0]
----

include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=font-icon]
----

TIP: The `fontIcon` component can be nested within the `prefix`, `suffix`, or `icon` elements.

[[svg-sprite]]
== SVG Sprite

An SVG sprite is a collection of SVG images loaded into an application as a single file. You can create a sprite manually or using one of many third-party tools.

To use an SVG sprite as an icon set:

. Create a JavaScript file for your icon set in the `/frontend/icons/` directory. The following code defines a sprite containing three icons:

+
.my-icons.js
[source,javascript,indent=0]
----
include::example$/onboarding/frontend/icons/my-icons.js[tags=my-icons-sprite]
----
+
. Create a Java enum that implements `IconFactory` and add the `@JsModule` annotation referring to the JavaScript file:
+
.MyIcons.java
[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/icons/MyIcons.java[tags=my-icons-enum]
----

After the icon set is registered, the icons can be used similarly to built-in icons:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=sprite]
----

image::icons/sprite-icons.png[align="center", width="125"]

Or set it to a component in the Java code:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/icons/IconsView.java[tags=programmatic-sprite]
----

[[standalone-svg-images]]
== Standalone SVG Images

SVG files can be added to your static content directory. By default, Spring Boot serves static resources from the following locations in the classpath: `/static`, `/public`, `/resources`, or `/META-INF/resources`. To organize icons, you can use a subfolder such as `META-INF/resources/icons`.

To serve SVGs, use the `svgIcon` component:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-standalone-1]
----

To set the icon to a component, use its nested `icon` element:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-standalone-2]
----

If the component supports xref:flow-ui:vc/miscellaneous/prefix-suffix.adoc[prefix or suffix], place the icon there to position it relative to content. For example, to put the icon before the button text:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/icons/icons-view.xml[tags=declarative-standalone-3]
----

You can also set SVG icons programmatically:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/icons/IconsView.java[tags=programmatic-standalone]
----