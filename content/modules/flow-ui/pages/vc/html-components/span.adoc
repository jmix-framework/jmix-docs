= span

`span` displays a short piece of text that can be styled independently.

[cols="1h,1" grid=none, width=50%, frame=none]
|===
|XML Element
|`span`

|Java Class
|`Span`
|===

[[basics]]
== Basics

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/html-declaratively" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Like its HTML counterpart, `span` adds and styles inline text. Each `span` is a separate component, so its styling applies to the entire value of its `text` attribute.

TIP: Use the xref:vc/components/html.adoc[html] component to include native HTML directly.

The example places several `span` elements in one list item so that individual parts of the sentence can have different styles:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/html/declaratively/html-declaratively.xml[tags=span-basics]
----

[[variants]]
== Theme Variants

Use the xref:vc/common-attributes.adoc#themeNames[themeNames] attribute to style the text as a badge. Combine `badge` with color, emphasis, shape, or size variants.

[cols="1,4,1", options="header"]
|===
|Variant
|Description
|Supported By

|`badge`
|Applies badge styling.
|Aura, Lumo

|`success`, `warning`, `error`, `contrast`
|Applies a semantic color.
|Aura, Lumo

|`primary`
|Adds stronger visual emphasis.
|Aura, Lumo

|`pill`
|Rounds the badge corners.
|Aura, Lumo

|`small`
|Makes the badge smaller.
|Aura, Lumo

|`normal`
|Uses the regular badge size.
|Aura, Lumo
|===

[[attributes]]
== Attributes

xref:vc/common-attributes.adoc[Common attributes] serve the same purpose for all components.
The following attributes are specific to `span`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default
|[[themeNames]]`themeNames`
|Applies one or more variants listed in <<variants,Theme Variants>>.
|—
|===

[[handlers]]
== Handlers

xref:vc/common-handlers.adoc[Common handlers] are configured in the same way for all components.
The following handlers are specific to `span`:

[cols="2,5", options="header"]
|===
|Name
|Description
|[[ClickEvent]]`ClickEvent`
|Fires when the component is clicked. Use the `click`, `singleClick`, or `doubleClick` subject to select the click pattern.
|===

== See Also

* https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/span[MDN Span documentation^]
