= split
:page-aliases: vcl/containers/split-panel.adoc

`split` creates two content areas separated by a draggable splitter.

[cols="1h,1" grid=none, width=50%, frame=none]
|===
|XML Element
|`split`

|Java Class
|`JmixSplitLayout`
|===

== Overview

image::visual-components/layouts/split-basic.png[align="center", width="445"]

[[basics]]
== Basics

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/split-simple" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

`split` must contain exactly two components or layouts. The example gives the primary area 45% of the available space with `splitterPosition="45"` and nests a vertical split in the secondary area. Users can drag either splitter to resize its areas.

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/containers/split/simple/split-simple.xml[tags=basics]
----

[[variants]]
== Theme Variants

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/split-theme-variant" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Use the xref:vc/common-attributes.adoc#themeNames[themeNames] attribute to set a component theme.

[cols="1,4,1", options="header"]
|===
|Variant
|Description
|Supported By

|`minimal`
|Hides the visual styling of the splitter.
|Aura, Lumo

|`small`
|Uses a thinner splitter that becomes visible on hover.
|Aura, Lumo

|`splitter-spacing`
|Adds spacing around the splitter.
|Aura, Lumo
|===

[[attributes]]
== Attributes

xref:vc/common-attributes.adoc[Common attributes] serve the same purpose for all components. The following attributes are specific to `split`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default

|[[orientation]]`orientation`
|Controls whether the content areas are arranged side by side (`HORIZONTAL`) or one above the other (`VERTICAL`).
|`HORIZONTAL`

|[[splitterPosition]]`splitterPosition`
|Sets the initial splitter position as a percentage of the layout size.
|—
|===

== Handlers

[cols="2,5", options="header"]
|===
|Name
|Description

|xref:vc/common-handlers.adoc#AttachEvent[`AttachEvent`]
|Fires when the layout is attached to the UI.

|`ClickEvent`
|Fires when the layout is clicked.

|xref:vc/common-handlers.adoc#DetachEvent[`DetachEvent`]
|Fires when the layout is detached from the UI.
|===

== Elements

A `split` must contain exactly two components or layouts: the primary and secondary content areas.

== See Also

* https://vaadin.com/docs/v25/components/split-layout[Vaadin Split Layout documentation^]
