= hbox
:page-aliases: vcl/containers/box-layout.adoc

`hbox` arranges nested components horizontally.

[cols="1h,1" grid=none, width=50%, frame=none]
|===
|XML Element
|`hbox`

|Java Class
|`HorizontalLayout`
|===

== Overview

image::visual-components/layouts/hbox-basic.png[align="center", width="385"]

[[basics]]
== Basics

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/hbox-simple" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

The example arranges several components in one row and aligns them along the cross axis with `alignItems`:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/containers/boxlayout/hbox/simple/hbox-simple.xml[tags=basics]
----

[[horizontal-alignment]]
[[aligning-individual]]
== Start, Middle, and End Slots

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/hbox-slots" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Use the `startSlot`, `middleSlot`, and `endSlot` elements to place components at the corresponding positions within the available width:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/containers/boxlayout/hbox/slots/hbox-slots.xml[tags=slots]
----

[[justification]]
== Content Justification

Justification modes define how items are distributed within the available horizontal space. The following modes are supported:

* `START` (default)
+
Aligns items to the left (for left-to-right languages) or to the right (for right-to-left languages).

* `CENTER`
+
Centers all items horizontally within the layout.

* `END`
+
Aligns items to the right (for left-to-right languages) or to the left (for right-to-left languages).

* `BETWEEN`
+
Distributes available space evenly between items. No extra space is added before the first item or after the last.

* `AROUND`
+
Distributes space evenly around items. The space before the first item and after the last is half the space between adjacent items.

* `EVENLY`
+
Distributes space evenly between all items, including before the first and after the last, ensuring equal spacing throughout.

Use the <<justifyContent,justifyContent>> attribute to set the justify content mode of the layout.

For usage examples, see xref:vc/layout-rules.adoc#justify-content-mode[Layout Rules].

[[attributes]]
== Attributes

xref:vc/common-attributes.adoc[Common attributes] serve the same purpose for all components.

The following attributes are specific to `hbox`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default
|[[boxSizing]]boxSizing
|Sets how the total width and height of an element is calculated. Possible values are: `UNDEFINED`, `CONTENT_BOX`, `BORDER_BOX`. See also: https://developer.mozilla.org/en-US/docs/Web/CSS/box-sizing[MDN page about box-sizing^].
|`UNDEFINED`
|[[expand]]expand
|Specifies the ID of a child component whose flex-grow value is set to `1`, allowing it to take up remaining horizontal space.
|—
|[[justifyContent]]justifyContent
|Defines how the browser distributes space between and around content items. See <<justification,Justification>>.
|`START`
|[[margin]]margin
|Adds margin outside the layout on all four sides. See https://developer.mozilla.org/en-US/docs/Web/CSS/margin[margin^] on MDN.
|`false`
|[[padding]]padding
|Adds padding inside the layout on all four sides. See https://developer.mozilla.org/en-US/docs/Web/CSS/padding[padding^] on MDN.
|`false`
|[[spacing]]spacing
|Adds theme-defined spacing between child components.
|`true`
|[[wrap]]wrap
|When `true`, moves components to a new row when horizontal space runs out. See https://vaadin.com/docs/v25/components/horizontal-layout#wrapping[Wrapping] in the Vaadin documentation.
|`false`
|===

[[handlers]]
== Handlers

[cols="2,5", options="header"]
|===
|Name
|Description

|xref:vc/common-handlers.adoc#AttachEvent[`AttachEvent`]
|Fires when the layout is attached to the UI.

|[[ClickEvent]]`ClickEvent`
|Fires when the layout is clicked.

|xref:vc/common-handlers.adoc#DetachEvent[`DetachEvent`]
|Fires when the layout is detached from the UI.
|===

== Elements

An `hbox` can contain components directly or group them in `startSlot`, `middleSlot`, and `endSlot` elements.

== See Also

* https://vaadin.com/docs/v25/components/horizontal-layout[Vaadin Horizontal Layout documentation^]
