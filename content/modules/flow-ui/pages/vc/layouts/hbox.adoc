= hbox
:page-aliases: vcl/containers/box-layout.adoc

`hbox` располагает вложенные компоненты горизонтально.

[cols="1h,1" grid=none, width=50%, frame=none]
|===
|XML-элемент
|`hbox`

|Java-класс
|`HorizontalLayout`
|===

== Обзор

image::visual-components/layouts/hbox-basic.png[align="center", width="385"]

[[basics]]
== Основы

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.ru/ui-samples/sample/hbox-simple" class="live-demo-btn" target="_blank">ДЕМО</a>
</div>
++++

В примере несколько компонентов расположены в одну строку и выровнены по поперечной оси с помощью `alignItems`:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/containers/boxlayout/hbox/simple/hbox-simple.xml[tags=basics]
----

[[horizontal-alignment]]
[[aligning-individual]]
== Слоты начала, середины и конца

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.ru/ui-samples/sample/hbox-slots" class="live-demo-btn" target="_blank">ДЕМО</a>
</div>
++++

Используйте элементы `startSlot`, `middleSlot` и `endSlot`, чтобы разместить компоненты в соответствующих позициях по доступной ширине:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/containers/boxlayout/hbox/slots/hbox-slots.xml[tags=slots]
----

[[justification]]
== Выравнивание содержимого

Режимы выравнивания определяют, как элементы распределяются в доступном горизонтальном пространстве. Поддерживаются следующие режимы:

* `START` (по умолчанию)
+
Выравнивает элементы по левому краю (для языков с письмом слева направо) или по правому краю (для языков с письмом справа налево).

* `CENTER`
+
Выравнивает все элементы по центру контейнера по горизонтали.

* `END`
+
Выравнивает элементы по правому краю (для языков с письмом слева направо) или по левому краю (для языков с письмом справа налево).

* `BETWEEN`
+
Равномерно распределяет доступное пространство между элементами. Дополнительное пространство не добавляется перед первым или после последнего элемента.

* `AROUND`
+
Равномерно распределяет пространство вокруг элементов. Пространство перед первым элементом и после последнего равно половине пространства между соседними элементами.

* `EVENLY`
+
Равномерно распределяет пространство между всеми элементами, включая перед первым и после последнего, обеспечивая одинаковые промежутки.

Используйте атрибут <<justifyContent,justifyContent>> для установки режима выравнивания содержимого контейнера.

Примеры использования приведены в разделе xref:vc/layout-rules.adoc#justify-content-mode[Правила компоновки].

[[attributes]]
== Атрибуты

В Jmix существует множество xref:vc/common-attributes.adoc[общих атрибутов], которые служат одной цели для всех компонентов.

Следующие атрибуты специфичны для `hbox`:

[cols="2,5,1", options="header"]
|===
|Имя
|Описание
|По умолчанию
|[[boxSizing]]boxSizing
|Задает способ вычисления общей ширины и высоты элемента. Возможные значения: `UNDEFINED`, `CONTENT_BOX`, `BORDER_BOX`. Смотрите также: https://developer.mozilla.org/en-US/docs/Web/CSS/box-sizing[страницу MDN о box-sizing^].
|`UNDEFINED`
|[[expand]]expand
|Задаёт идентификатор дочернего компонента, для которого значение `flex-grow` устанавливается в `1`. Благодаря этому компонент занимает оставшееся пространство по горизонтали.
|—
|[[justifyContent]]justifyContent
|Определяет, как браузер распределяет пространство между элементами содержимого и вокруг них. Смотрите <<justification,Выравнивание содержимого>>.
|`START`
|[[margin]]margin
|Добавляет внешние отступы со всех четырёх сторон контейнера. См. https://developer.mozilla.org/en-US/docs/Web/CSS/margin[описание margin^] на MDN.
|`false`
|[[padding]]padding
|Добавляет внутренние отступы со всех четырёх сторон контейнера. См. https://developer.mozilla.org/en-US/docs/Web/CSS/padding[описание padding^] на MDN.
|`false`
|[[spacing]]spacing
|Добавляет между дочерними компонентами интервалы, заданные темой.
|`true`
|[[wrap]]wrap
|Если установлено значение `true`, переносит компоненты в новую строку, когда заканчивается пространство по горизонтали. См. раздел https://vaadin.com/docs/v25/components/horizontal-layout#wrapping[Wrapping^] в документации Vaadin.
|`false`
|===

[[handlers]]
== Обработчики

[cols="2,5", options="header"]
|===
|Имя
|Описание

|xref:vc/common-handlers.adoc#AttachEvent[`AttachEvent`]
|Срабатывает при присоединении контейнера к UI.

|[[ClickEvent]]`ClickEvent`
|Срабатывает при нажатии на контейнер.

|xref:vc/common-handlers.adoc#DetachEvent[`DetachEvent`]
|Срабатывает при отсоединении контейнера от UI.
|===

== Элементы

`hbox` может содержать компоненты напрямую или группировать их в элементах `startSlot`, `middleSlot` и `endSlot`.

== Смотрите также

* https://vaadin.com/docs/v25/components/horizontal-layout[Документация Vaadin Horizontal Layout^]
