= loginForm

The `loginForm` component collects credentials and related authentication options.

[cols="1h,1", options="autowidth", grid=none, frame=none]
|===
|XML Element
|`loginForm`

|Java Class
|`JmixLoginForm`
|===

== Overview

[cols="a", options="autowidth", frame=all, grid=none, align=center]
|===
|image::visual-components/components/login-form.png[Login form, align="center", width="320"]
|===

[[basics]]
== Basics

A basic `loginForm` contains username and password fields and a login button. It can also show a locale selector, a *Remember me* checkbox, and a *Forgot password* button.

[[labels-messages]]
=== Labels and Messages

Use the nested `form` element to set the form title, field labels, and button text. Its values can be literal text or xref:localization:message-bundles.adoc[message-bundle] keys. The example also defines `errorMessage` content for failed authentication and `additionalInformation` content below the form:

[source,xml,indent=0]
----
include::example$onboarding/src/main/resources/com/company/onboarding/view/component/loginform/login-form-view.xml[tags=custom]
----

=== Optional Controls

The visibility attributes let you show or hide the locale selector, *Remember me* checkbox, and *Forgot password* button without changing the nested form content:

[source,xml,indent=0]
----
include::example$onboarding/src/main/resources/com/company/onboarding/view/component/loginform/login-form-view.xml[tags=basic]
----

[[remember-me]]
=== Remember Me

The *Remember me* option stores a persistent browser token so that an authenticated session can survive beyond the HTTP session and application restart. Read the selected state from `LoginEvent` when authenticating, and use <<RememberMeChangedEvent,RememberMeChangedEvent>> when other UI behavior must react immediately to checkbox changes.

See https://docs.spring.io/spring-security/reference/servlet/authentication/rememberme.html[Spring Security Remember-Me documentation^] for details about the underlying authentication mechanism.

[[authentication]]
== Authentication

The standard login view subscribes to `LoginEvent`, applies the selected locale, and passes the entered credentials and remember-me state to `AuthenticationManager`. It reports authentication failures through the form's error state:

[source,java,indent=0]
----
include::example$onboarding/src/main/java/com/company/onboarding/view/login/LoginView.java[tags=login-form;on-login-handler]
----
<1> Injects the form so the controller can update its state.
<2> Applies the locale selected in the form.
<3> Passes the checkbox value to the authentication token.

== Authentication Error Handling

Extend the login handler when users need a more specific explanation than a general authentication failure. This example detects a locked account, displays an appropriate message, and records the failed attempt:

[source,java,indent=0]
----
include::example$onboarding/src/main/java/com/company/onboarding/view/component/loginform/LoginFormView.java[tags=exception-handling]
----
<1> Chooses the error message for a `LockedException`.
<2> Logs the failed login attempt.

== Styled Login View

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/custom-login" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

The sample combines the form descriptor with component-scoped CSS. XML provides the login content and CSS controls the surrounding layout, background image, and form positioning:

.XML
[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/cookbook/customlogin/custom-login.xml[tags=custom-login]
----

.CSS
[source,css,indent=0]
----
include::example$ui-samples/main/resources/META-INF/resources/themes/lumo/samples/components/custom-login.css[tags=custom-login]
----

== Attributes

The following attributes are specific to `loginForm`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default

|[[forgotPasswordButtonVisible]]`forgotPasswordButtonVisible`
|Shows the *Forgot password* button.
|`false`

|[[localesVisible]]`localesVisible`
|Shows the locale selector.
|`false`

|[[rememberMeVisible]]`rememberMeVisible`
|Shows the *Remember me* checkbox.
|`false`
|===

The following shared attributes are supported by `loginForm`:

xref:vc/common-attributes.adoc#id[id] -
xref:vc/common-attributes.adoc#enabled[enabled] -
xref:vc/common-attributes.adoc#visible[visible] -
xref:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:vc/common-attributes.adoc#colspan[colspan] -
xref:vc/common-attributes.adoc#classNames[classNames] -
xref:vc/common-attributes.adoc#css[css]

== Handlers

The following handlers are specific to `loginForm`:

[cols="2,5", options="header"]
|===
|Name
|Description

|[[ForgotPasswordEvent]]ForgotPasswordEvent
|Fired when the user clicks *Forgot password*.

|[[LocaleChangedEvent]]LocaleChangedEvent
|Fired when the user selects a different locale.

|[[LoginEvent]]LoginEvent
|Fired when the user submits the entered credentials.

|[[RememberMeChangedEvent]]RememberMeChangedEvent
|Fired when the *Remember me* checkbox state changes.

|[[localeItemLabelGenerator]]`localeItemLabelGenerator`
|Provides the label displayed for each locale in the selector.
|===

The following shared handlers are supported by `loginForm`:

xref:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:vc/common-handlers.adoc#DetachEvent[DetachEvent]

== Elements

A `loginForm` can contain `form`, `errorMessage`, and `additionalInformation` elements. The `form` element defines labels and button text, `errorMessage` defines the authentication-failure content, and `additionalInformation` adds supporting content below the form.

== See Also

* https://vaadin.com/docs/v25/components/login[Vaadin Login documentation^]
