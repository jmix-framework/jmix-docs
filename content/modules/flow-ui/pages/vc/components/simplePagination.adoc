= simplePagination
:page-aliases: vcl/components/pagination.adoc, vcl/components/simple-pagination.adoc

`simplePagination` loads data in pages and provides controls for navigating between them.

[cols="1h,1", options="autowidth", grid=none, frame=none]
|===
|XML Element
|`simplePagination`

|Java Class
|`SimplePagination`
|===

== Overview

[cols="a", options="autowidth", frame=all, grid=none, align=center]
|===
|image::visual-components/components/simple-pagination.png[Simple pagination, align="center", width="450"]
|===

[[basics]]
== Basics

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/simple-pagination-simple" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

`simplePagination` has a simple view with a count of rows and navigation buttons.

It can also have a <<items-per-page,drop-down list>> for the number of items per page.

By default, Jmix Studio generates `simplePagination` when creating xref:flow-ui:views/view-classes.adoc#standard-list-view[StandardListView] for an entity.

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/simplepagination/simple/simple-pagination-simple.xml[tags=basics]
----

[[items-per-page]]
== Items per Page

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/simple-pagination-items-per-page" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

[[itemsPerPageVisible]]
`simplePagination` has a special `comboBox` with options to limit the number of items for one page. To make it visible, set the `itemsPerPageVisible` attribute to the `true` value. The default value is `false`.

The default value of this list is specified in the xref:flow-ui:ui-properties.adoc#jmix.ui.component.pagination-items-per-page-items[jmix.ui.component.pagination-items-per-page-items] property.

[[itemsPerPageItems]]
You can configure a custom list of options using the `itemsPerPageItems` attribute. The value of the attribute should be a comma-separated list of options:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/simplepagination/itemsperpage/simple-pagination-items-per-page.xml[tags=items-per-page]
----

Options that are less than or equal to `0` are ignored. Options that are greater than entity’s <<max-fetch-size,maximum fetch size>> are replaced by that maximum value.

[[itemsPerPageDefaultValue]]
Use the `itemsPerPageDefaultValue` attribute to set a default value from the list of options:

[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/simplepagination/itemsperpage/simple-pagination-items-per-page.xml[tags=items-per-page]
----

[[itemsPerPageUnlimitedItemVisible]]
The `itemsPerPageUnlimitedItemVisible` attribute sets the visibility of unlimited (null) option value in the items per page `comboBox`. The default value is `true`.

When the null option is selected in the `comboBox`, the component will try to load all data it can with the current maximum fetch size limitation.

[[max-fetch-size]]
[NOTE]
====
The maximum fetch size for all entities is defined by xref:flow-ui:ui-properties.adoc#jmix.ui.default-max-fetch-size[jmix.ui.default-max-fetch-size] UI property. Its default value is 10000. A particular entity may have different maximum fetch size, set with xref:flow-ui:ui-properties.adoc#jmix.ui.entity-max-fetch-size[jmix.ui.entity-max-fetch-size].
====

[[items-per-page-empty-label]]
== Empty Item Label

When <<itemsPerPageUnlimitedItemVisible,itemsPerPageUnlimitedItemVisible>>=`"true"`, the dropdown list shows an empty item that allows loading all data (limited by the maximum fetch size). By default, this item is displayed as an empty string.

You can customize the label for this empty item by adding a message key to your xref:localization:message-bundles.adoc[message bundle]:

[source,properties]
----
pagination.itemsPerPage.emptySelectionCaption=Show all
----

[NOTE]
====
If the message key `pagination.itemsPerPage.emptySelectionCaption` is present in the message bundle, its value will be displayed instead of an empty item in the dropdown list. If the key is not found, the item remains empty.
====

[[before-refresh]]
== Preventing a Refresh

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/simple-pagination-before-refresh" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

`BeforeRefreshEvent` is fired before the associated data loader loads another page. A handler can call `preventRefresh()` to cancel the operation.

.XML
[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/simplepagination/beforerefresh/simple-pagination-before-refresh.xml[tags=before-refresh]
----

.Java
[source,java,indent=0]
----
include::example$ui-samples/main/java/io/jmix/uisamples/view/flowui/components/simplepagination/beforerefresh/SimplePaginationBeforeRefreshSample.java[tags=before-refresh]
----

[[after-refresh]]
== Handling a Completed Refresh

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/simple-pagination-after-refresh" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

`AfterRefreshEvent` is fired after the component successfully refreshes data, provided that the operation was not prevented.

.XML
[source,xml,indent=0]
----
include::example$ui-samples/main/resources/io/jmix/uisamples/view/flowui/components/simplepagination/afterrefresh/simple-pagination-after-refresh.xml[tags=after-refresh]
----

.Java
[source,java,indent=0]
----
include::example$ui-samples/main/java/io/jmix/uisamples/view/flowui/components/simplepagination/afterrefresh/SimplePaginationAfterRefreshSample.java[tags=after-refresh]
----

== Attributes

The following attributes are specific to `simplePagination`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default

|[[autoLoad]]`autoLoad`
|The `autoLoad` attribute determines whether the item count should be automatically loaded.
|—

|[[dataLoader]]`dataLoader`
|To create `simplePagination` connected to data, use the `dataLoader` attribute.
|—

|`itemsPerPageDefaultValue`
|Sets the items per page default value. See <<items-per-page,Items per Page>>.
|—

|`itemsPerPageItems`
|Sets the items per page items. See <<items-per-page,Items per Page>>.
|—

|`itemsPerPageUnlimitedItemVisible`
|Controls whether the items-per-page list includes an option to load all available items. See <<items-per-page,Items per Page>>.
|—

|`itemsPerPageVisible`
|Controls whether the items-per-page selector is displayed. See <<items-per-page,Items per Page>>.
|—
|===

The following shared attributes are supported by `simplePagination`:

xref:vc/common-attributes.adoc#id[id] -
xref:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:vc/common-attributes.adoc#classNames[classNames] -
xref:vc/common-attributes.adoc#colspan[colspan] -
xref:vc/common-attributes.adoc#css[css] -
xref:vc/common-attributes.adoc#visible[visible]

== Handlers

The following handlers are specific to `simplePagination`:

[cols="2,5", options="header"]
|===
|Name
|Description

|[[AfterRefreshEvent]]`AfterRefreshEvent`
|`io.jmix.flowui.component.PaginationComponent.AfterRefreshEvent` is fired after data refresh.

|[[BeforeRefreshEvent]]`BeforeRefreshEvent`
|`io.jmix.flowui.component.PaginationComponent.BeforeRefreshEvent` is fired before refreshing the data when the user clicks next, previous, etc.

|[[totalCountDelegate]]`totalCountDelegate`
|Sets delegate which is used to get the total count of items.
|===

The following shared handlers are supported by `simplePagination`:

xref:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:vc/common-handlers.adoc#DetachEvent[DetachEvent]
