= geoMap
:page-aliases: maps:map-component.adoc

[NOTE.addon-component]
====
Для работы компонента требуется коммерческое дополнение xref:maps:index.adoc#installation[Maps].
====

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.ru/ui-samples/sample/maps" class="live-demo-btn" target="_blank">ДЕМО</a>
</div>
++++

`geoMap` отображает географические данные.

[cols="1h,1", options="autowidth", grid=none, frame=none]
|===
|XML-элемент
|`geoMap`

|Java-класс
|`GeoMap`
|===

[[basics]]
== Основы

[[map]]

Объявите пространство имен `maps` в XML-дескрипторе экрана:

[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=xmlns]
----

[TIP]
====
Studio автоматически добавляет пространство имен, когда вы добавляете компонент с помощью действия *Add Component* на верхней панели действий. См. xref:studio:view-designer.adoc#component-palette[Палитра компонентов].
====


Затем добавьте элемент `geoMap`:

[source,xml,indent=0]
----
<maps:geoMap id="map"
             height="100%"
             width="100%"/>
----

[[how-to-inject]]
Если задан атрибут `id`, компонент `GeoMap` можно инжектировать в контроллер и обращаться непосредственно к его API:

[source,java,indent=0]
----
include::maps:example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=geoMap;onInit]
----

[[layer]]
== Слой

Компонент `GeoMap` может включать несколько слоев, отображающих разные типы географической информации.
Изначально карта не содержит слоев.

Дополнение Maps предоставляет следующие типы слоев:

* xref:maps:layers-sources.adoc#tile-layer[TileLayer] отображает предварительно отрисованные тайловые изображения, организованные по уровням масштабирования.
* xref:maps:layers-sources.adoc#image-layer[ImageLayer] отображает статические изображения или изображения, полученные от Web Map Service (WMS).
* xref:maps:layers-sources.adoc#vector-layer[VectorLayer] отображает объекты и геометрии.
* xref:maps:high-density-data-visualization.adoc#heatmap[HeatmapLayer] отображает точечные объекты в виде тепловой карты.

Описание источников, поддерживаемых каждым типом слоя, и примеры использования приведены в разделе xref:maps:layers-sources.adoc[Слои и источники].

Слой можно инжектировать в контроллер и использовать программно:

[source,java,indent=0]
----
include::maps:example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=mapTile;onInit]
----

[[source]]
== Источник

xref:maps:layers-sources.adoc[Источник] содержит данные для отрисовки слоев карты. Источники предоставляют пространственную информацию, которая определяет содержимое и внешний вид слоев.

Дополнение Maps предоставляет следующие типы источников:

* Для `TileLayer`: xref:maps:layers-sources.adoc#osm-source[`OsmSource`], xref:maps:layers-sources.adoc#xyz-source[`XyzSource`] и xref:maps:layers-sources.adoc#wms-source[`WmsSource`].
* Для `ImageLayer`: xref:maps:layers-sources.adoc#image-static-source[`ImageStaticSource`] и xref:maps:layers-sources.adoc#image-wms-source[`ImageWmsSource`].
* Для `VectorLayer`: xref:maps:layers-sources.adoc#vector-source[`VectorSource`], xref:maps:layers-sources.adoc#data-vector-source[`DataVectorSource`] и xref:maps:high-density-data-visualization.adoc#cluster-source[`ClusterSource`].
* Для `HeatmapLayer`: xref:maps:high-density-data-visualization.adoc#heatmap[`HeatmapDataVectorSource`] и `VectorSource`.

Описание источников и примеры их использования приведены в разделе xref:maps:layers-sources.adoc[Слои и источники].

В следующем примере для тайлового слоя задается `OsmSource`:

[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=osmSource]
----

Источник можно <<how-to-inject,инжектировать>> в контроллер и использовать программно:

[source,java,indent=0]
----
include::maps:example$/maps-ex2/src/main/java/com/company/mapssample/view/mapbasic/MapBasicView.java[tags=osmSource;onInit]
----

[[view]]
== Отображение

https://openlayers.org/en/v8.2.0/apidoc/module-ol_View-View.html[View^] определяет отображение карты: ее центр, уровень масштабирования, поворот и проекцию. Таким образом задается начальное состояние карты при загрузке в пользовательском интерфейсе.

По умолчанию `geoMap` отображает карту мира с начальным географическим центром в точке `(0,0)`.

[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=mapView]
----

Можно указать следующие дополнительные параметры:

* `centerX` определяет широту начального географического центра карты. Значение передается в объект `org.locationtech.jts.geom.Coordinate`.
* `centerY` определяет долготу начального географического центра карты. Значение передается в объект `org.locationtech.jts.geom.Coordinate`.
* `maxZoom` задает максимальный уровень масштабирования.
* `minZoom` задает минимальный уровень масштабирования.
* `projection` определяет систему координат (CRS), используемую для отображения географических данных. Она преобразует трехмерные координаты Земли в двумерную плоскость. Распространенные проекции: `EPSG:3857` (Web Mercator) и `EPSG:4326` (WGS 84). По умолчанию используется https://epsg.io/3857[EPSG:3857^]. Можно задать <<custom-projection,пользовательскую проекцию>>.
* `rotation` задает поворот в радианах: положительное значение поворачивает карту по часовой стрелке, а `0` соответствует направлению на север. Подробнее см. https://openlayers.org/en/v8.2.0/apidoc/module-ol_View-View.html#setRotation[setRotation^].
* `zoom` задает уровень масштабирования. Значение `0` соответствует максимальному отдалению; при приближении значение увеличивается.

[[custom-projection]]
== [[set-custom-projection]]Пользовательская проекция

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.ru/ui-samples/sample/geo-map-custom-projection" class="live-demo-btn" target="_blank">ДЕМО</a>
</div>
++++

Чтобы настроить пользовательскую проекцию, выполните следующие действия:

. *Определите бин проекции.* Зарегистрируйте проекцию в Spring-конфигурации в формате https://epsg.io/21781.proj4[PROJ.4^]:
+
[source,java,indent=0]
----
include::maps:example$/maps-ex2/src/main/java/com/company/mapssample/MapsEx2Application.java[tags=register-projection]
----

. *Настройте компонент карты.* Укажите атрибут `projection` элемента `mapView` в XML-дескрипторе:
+
[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/customprojection/custom-projection-view.xml[tags=map]
----

. *Инициализируйте проекцию в контроллере.* Программно примените проекцию ко всем компонентам карты на странице:
+
[source,java,indent=0]
----
include::maps:example$/maps-ex2/src/main/java/com/company/mapssample/view/customprojection/CustomProjectionView.java[tags=set-projection]
----
+
NOTE: Проекция применяется ко всем компонентам карты на странице. После ее установки все операции с координатами используют единицы измерения этой проекции: метры, градусы и т. д.

[[extent]]
== Охват

https://openlayers.org/en/v8.2.0/apidoc/module-ol_extent.html#~Extent[Охват^] (extent) задаёт географические границы <<view,отображения>> карты или определённого <<layer,слоя>> и тем самым определяет видимую область карты.

Охват задаётся минимальными координатами нижнего левого угла и максимальными координатами верхнего правого угла в формате [`minX`, `minY`, `maxX`, `maxY`].

Выберите слой или элемент `mapView` на панели структуры *Jmix UI* либо в XML-дескрипторе экрана, нажмите *Add* на панели инспектора, выберите *Extent* и задайте атрибуты `minX`, `minY`, `maxX` и `maxY`:

[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapbasic/map-basic-view.xml[tags=extent]
----

[[extent-constraint-options]]
=== Параметры ограничения охвата

Если для `mapView` задан `extent`, OpenLayers по умолчанию ограничивает всю область просмотра этим охватом. В адаптивных макетах карта может принудительно увеличиваться, если соотношение сторон контейнера отличается от соотношения сторон охвата.

Для настройки поведения используйте следующие атрибуты `mapView`:

* `showFullExtent` позволяет уменьшить масштаб настолько, чтобы показать весь заданный охват, даже если соотношение сторон контейнера отличается. Значение по умолчанию — `false`.
* `constrainOnlyCenter` применяет ограничение охвата только к центру области просмотра, поэтому её края могут выходить за пределы охвата. Значение по умолчанию — `false`.
* `smoothExtentConstraint` применяет ограничение плавно, допуская небольшое смещение за границу с эффектом возврата вместо жесткой остановки. Значение по умолчанию — `true`.

Эти атрибуты задаются при создании. Они передаются в представление OpenLayers во время инициализации и не могут изменяться во время выполнения.

[source,xml,indent=0]
----
include::maps:example$/maps-ex2/src/main/resources/com/company/mapssample/view/mapcomponent/map-component-view.xml[tags=extentConstraintOptions]
----

== Атрибуты

`geoMap` не определяет собственных атрибутов.

Компонент поддерживает следующие общие атрибуты:

xref:flow-ui:vc/common-attributes.adoc#id[id] -
xref:flow-ui:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:flow-ui:vc/common-attributes.adoc#classNames[classNames] -
xref:flow-ui:vc/common-attributes.adoc#colspan[colspan] -
xref:flow-ui:vc/common-attributes.adoc#css[css] -
xref:flow-ui:vc/common-attributes.adoc#height[height] -
xref:flow-ui:vc/common-attributes.adoc#maxHeight[maxHeight] -
xref:flow-ui:vc/common-attributes.adoc#maxWidth[maxWidth] -
xref:flow-ui:vc/common-attributes.adoc#minHeight[minHeight] -
xref:flow-ui:vc/common-attributes.adoc#minWidth[minWidth] -
xref:flow-ui:vc/common-attributes.adoc#visible[visible] -
xref:flow-ui:vc/common-attributes.adoc#width[width]

== Обработчики

Следующие события относятся к `geoMap`:

include::flow-ui:vc/handler-generation-tip.adoc[]

[cols="2,5", options="header"]
|===
|Название
|Описание

|[[MapClickEvent]]`MapClickEvent`
|Возникает при щелчке по карте. Двойной щелчок вызывает это событие дважды.

|[[MapDoubleClickEvent]]`MapDoubleClickEvent`
|Возникает при двойном щелчке по карте.

|[[MapMoveEndEvent]]`MapMoveEndEvent`
|Возникает после завершения перемещения карты.

|[[MapSingleClickEvent]]`MapSingleClickEvent`
|Возникает при одиночном щелчке после того, как компонент определит, что взаимодействие не является двойным щелчком.

|[[MapZoomChangedEvent]]`MapZoomChangedEvent`
|Возникает при изменении уровня масштабирования карты.
|===

Компонент поддерживает следующие общие обработчики:

xref:flow-ui:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:flow-ui:vc/common-handlers.adoc#DetachEvent[DetachEvent]

== Элементы

`geoMap` может содержать один элемент `mapView` и обязательный элемент `layers`. Элемент `layers` может содержать слои `tile`, `image`, `vector` и `heatmap` с источниками и элементами оформления, поддерживаемыми соответствующим типом слоя.

== См. также

* xref:maps:layers-sources.adoc[]
* xref:maps:events.adoc[]
* xref:maps:reference.adoc[]
