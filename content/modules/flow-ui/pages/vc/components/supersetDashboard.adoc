= supersetDashboard
:page-aliases: superset:dashboard-component.adoc

[NOTE.addon-component]
====
Для работы компонента требуется дополнение xref:superset:index.adoc#installation[Superset].
====

`supersetDashboard` встраивает в экраны приложения дэшборды, настроенные в Apache Superset.

[cols="1h,1", options="autowidth", grid=none, frame=none]
|===
|XML-элемент
|`dashboard`

|Java-класс
|`SupersetDashboard`
|===

[[basics]]
== [[usage]]Основы

Объявите пространство имен `superset` в XML-дескрипторе экрана:

[source,xml,indent=0]
----
include::superset:example$superset-ex2/src/main/resources/com/company/supersetsample/view/samples/dashboard/dashboard-view.xml[tags=superset-schema]
----

[TIP]
====
Studio автоматически добавляет пространство имен, когда вы добавляете компонент с помощью действия *Add Component* на верхней панели действий. См. xref:studio:view-designer.adoc#component-palette[Палитра компонентов].
====


Добавьте `supersetDashboard` в экран и укажите его `id`, размеры и полученный из Apache Superset идентификатор <<embeddedId,embeddedId>>:

[source,xml,indent=0]
----
include::superset:example$superset-ex2/src/main/resources/com/company/supersetsample/view/samples/dashboard/dashboard-view.xml[tags=dashboard]
----

Компонент также можно инжектировать в контроллер и использовать программно:

[source,java,indent=0]
----
include::superset:example$superset-ex2/src/main/java/com/company/supersetsample/view/samples/dashboard/DashboardView.java[tags=setting-embedded-id]
----

[[dataset-constraints]]
[[datasetConstraints]]
== Ограничения наборов данных

Дэшборд в Superset может содержать несколько диаграмм, отображающих данные из различных наборов данных. Компонент `SupersetDashboard` обеспечивает возможность задавать ограничения для этих наборов данных. Ограничения могут быть определены статически в элементе XML компонента или вычислены динамически при работе приложения.

Для предоставления ограничения вам нужно указать ID набора данных и написать условие на native SQL, которое будет добавлено в выражение *WHERE* запроса набора данных.

TIP: В интерфейсе Superset не очевидно, где найти ID набора данных. Вы можете получить его из параметра `datasource_id` в URL, отображаемом в адресной строке при открытии набора данных из списка наборов данных.

[[static-dataset-constraints]]
[[staticDatasetConstraints]]
=== Статичные ограничения

Рассмотрим использование статичных ограничений набора данных в дэшборде `Employees' salaries`, созданном в разделе xref:superset:getting-started.adoc[Начало работы]. Он использует набор данных, загружающий сотрудников, отделы и зарплаты. Предположим, что нужно ограничить нижнюю границу зарплаты, например, до `80 000`.

Ограничения определяются в элементах `datasetConstraint` вложенных в элемент `dashboard` компонента. Они могут быть добавлены с помощью действия *Add* в Студии или вручную в XML.

Требуемое определение ограничения будет выглядеть следующим образом:

[source, xml, indent=0]
----
include::superset:example$superset-ex2/src/main/resources/com/company/supersetsample/view/samples/staticdatasetconstraint/static-datasetconstraint-view.xml[tags=static-datasetconstraint]
----

Здесь `salary` — столбец набора данных.

[[dataset-constraints-provider]]
=== Провайдер ограничений

Ограничения набора данных могут быть рассчитаны динамически в работающем приложении и переданы в Superset при открытии дэшборда в приложении Jmix. Это позволяет фильтровать данные дэшборда в зависимости от прав доступа текущего пользователя или любых других критериев.

Ограничение набора данных определяется классом `DatasetConstraint`. Вы можете предоставить список ограничений компоненту `SupersetDashboard` следующими способами:

* Создать обработчик `datasetConstraintsProvider` в экране и вернуть список ограничений из него.

* Создать Spring-бин, реализующий интерфейс `DatasetConstraintsProvider`, и передать его компоненту с помощью метода `setDatasetConstraintsProvider()`.

Рассмотрим следующее требование: менеджер отдела может видеть информацию о зарплатах только в своем отделе. Ограничение набора данных может учитывать роль уровня строк, назначенную текущему пользователю.

В примере ниже используется первый подход с обработчиком `datasetConstraintsProvider` в экране, но логика вынесена в обычный Spring-бин:

[source, java, indent=0]
----
include::superset:example$superset-ex2/src/main/java/com/company/supersetsample/app/DepartmentDatasetConstraintProvider.java[]
----

Бин используется в обработчике `datasetConstraintsProvider` экрана, который может быть сгенерирован из панели инспектора Jmix:

[source, java, indent=0]
----
include::superset:example$superset-ex2/src/main/java/com/company/supersetsample/view/samples/staticdatasetconstraint/StaticDatasetconstraintView.java[tags=dataset-constraint-provider]
----

== Атрибуты

Следующие атрибуты относятся к `dashboard`:

[cols="2,5a,1", options="header"]
|===
|Название
|Описание
|Значение по умолчанию

|[[chartControlsVisible]]`chartControlsVisible`
|Определяет видимость элементов управления диаграммами.

image::superset:chart-controls-example.png[]
|`false`

|[[embeddedId]]`embeddedId`
|Задает идентификатор встроенного дэшборда из Superset. См. xref:superset:dashboard.adoc#create-dashboard[Создание дэшборда].

Идентификатор необходим для получения гостевого токена и загрузки дэшборда. При изменении идентификатора компонент перезагружает дэшборд; если идентификатор не задан, компонент отображает изображение-заглушку.

NOTE: Включите флаг `EMBEDDED_SUPERSET`, как описано в разделе xref:superset:configuration.adoc#enable-embedding-dashboards[Встраиваемые дэшборды].
|—

|[[filtersExpanded]]`filtersExpanded`
|Определяет, развернута ли панель фильтров.
|`false`

|[[titleVisible]]`titleVisible`
|Определяет видимость панели заголовка.

image::superset:title-visible-example.png[align="center"]
|`false`
|===

Компонент поддерживает следующие общие атрибуты:

xref:flow-ui:vc/common-attributes.adoc#id[id] -
xref:flow-ui:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:flow-ui:vc/common-attributes.adoc#classNames[classNames] -
xref:flow-ui:vc/common-attributes.adoc#colspan[colspan] -
xref:flow-ui:vc/common-attributes.adoc#css[css] -
xref:flow-ui:vc/common-attributes.adoc#height[height] -
xref:flow-ui:vc/common-attributes.adoc#maxHeight[maxHeight] -
xref:flow-ui:vc/common-attributes.adoc#maxWidth[maxWidth] -
xref:flow-ui:vc/common-attributes.adoc#minHeight[minHeight] -
xref:flow-ui:vc/common-attributes.adoc#minWidth[minWidth] -
xref:flow-ui:vc/common-attributes.adoc#visible[visible] -
xref:flow-ui:vc/common-attributes.adoc#width[width]

== Обработчики

Следующий обработчик относится к `dashboard`:

include::flow-ui:vc/handler-generation-tip.adoc[]

[cols="2,5", options="header"]
|===
|Название
|Описание

|[[datasetConstraintsProvider]]`datasetConstraintsProvider`
|Динамически предоставляет ограничения наборов данных, когда дэшборд запрашивает гостевой токен. См. <<datasetConstraintsProvider,Провайдер ограничений>>.
|===

Компонент поддерживает следующие общие обработчики:

xref:flow-ui:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:flow-ui:vc/common-handlers.adoc#DetachEvent[DetachEvent]

== Элементы

`dashboard` может содержать элемент `datasetConstraints` с любым количеством элементов `datasetConstraint`. Для каждого `datasetConstraint` требуется атрибут `datasetId`, а его текстовое содержимое задает SQL-условие. См. <<staticDatasetConstraints,Статичные ограничения>>.
