= userMenu

`userMenu` отображает основную кнопку, по нажатию на которую открывается выпадающее меню со списком элементов.

[cols="1h,6",grid=none]
|===
|XML-элемент
|`userMenu`
|Java-класс
|`UserMenu`
|Атрибуты
|xref:vc/common-attributes.adoc#id[id] -
xref:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:vc/common-attributes.adoc#classNames[classNames] -
xref:vc/common-attributes.adoc#colspan[colspan] -
xref:vc/common-attributes.adoc#css[css] -
xref:vc/common-attributes.adoc#enabled[enabled] -
xref:vc/common-attributes.adoc#focusShortcut[focusShortcut] -
<<openOnHover,openOnHover>> -
xref:vc/components/comboBox.adoc#overlayClass[overlayClass] -
xref:vc/common-attributes.adoc#tabIndex[tabIndex] -
<<theme-variants,themeNames>> -
xref:vc/common-attributes.adoc#title[title] -
xref:vc/common-attributes.adoc#visible[visible]
|Обработчики
|xref:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:vc/common-handlers.adoc#BlurEvent[BlurEvent] -
xref:vc/common-handlers.adoc#DetachEvent[DetachEvent] -
xref:vc/common-handlers.adoc#FocusEvent[FocusEvent] -
<<UserChangedEvent,UserChangedEvent>> -
<<buttonRenderer,buttonRenderer>> -
<<headerRenderer,headerRenderer>>
|Элементы
|<<actionItem,actionItem>> -
<<componentItem,componentItem>> -
<<separator,separator>> -
<<textItem,textItem>> -
<<viewItem,viewItem>>
|===

== Основы

Основная кнопка отображает компоновку, представляющую текущего пользователя.
Пользователь нажимает эту кнопку, чтобы открыть выпадающее меню.

Выпадающее меню содержит список действий, каждое из которых представлено кликабельным элементом или подменю.

image::visual-components/components/user-menu-basic.png[align="center",width="240"]

Ниже приведен пример определения `userMenu` со стандартным рендерером кнопки и несколькими пунктами меню:

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=user-menu-basic]
----

== Элементы

`userMenu`, определенный в XML-дескрипторе, может содержать вложенные элементы:

[[actionItem]]
=== actionItem

Элемент `actionItem` создает связь между пунктом пользовательского меню и конкретным xref:actions/declarative-actions.adoc[action], который должен быть выполнен при щелчке по этому пункту.

Вы можете либо определить действие декларативно, либо использовать атрибут `ref`, чтобы сослаться на `id` уже определенного `action`.

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=actions;layout;user-menu-action]
----

<1> Декларативно определенное действие.
<2> Ссылка на существующее действие.

Когда пользователь нажимает `actionItem` в выпадающем меню, Jmix автоматически вызывает `action`.
Чтобы реализовать его логику, можно сгенерировать метод-обработчик `ActionPerformedEvent`:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/component/usermenu/UserMenuView.java[tags=notifications-bean;user-menu-action]
----

Фреймворк предоставляет предопределенные xref:actions/user-menu-actions.adoc[].

[[componentItem]]
=== componentItem

Элемент `componentItem` позволяет определить пользовательское внутреннее содержимое для `userMenu`.

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=user-menu-component]
----

С помощью Jmix Studio можно сгенерировать заготовку обработчика `UserMenuItem.HasClickListener.ClickEvent` для `componentItem`.

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/component/usermenu/UserMenuView.java[tags=notifications-bean;user-menu-component]
----

[[textItem]]
=== textItem

Элемент `textItem` содержит текст и значок.

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=user-menu-text]
----

С помощью Jmix Studio можно сгенерировать заготовку обработчика `UserMenuItem.HasClickListener.ClickEvent` для `textItem`.

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/component/usermenu/UserMenuView.java[tags=notifications-bean;user-menu-text]
----

[[viewItem]]
=== viewItem

Элемент `viewItem` позволяет открыть определенный экран.

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=user-menu-view]
----

<1> Открывает экран с указанным идентификатором.
<2> Открывает экран указанного класса в режиме `DIALOG`.

[[separator]]
=== separator

Элемент `separator` используется для визуального разделения элементов в выпадающем меню.

Разделители автоматически скрываются при следующих условиях:

. Несколько разделителей идут подряд. В этом случае видимым остается только один разделитель.
. Разделитель является первым дочерним элементом, а рендерер заголовка не определен.
. Разделитель является последним дочерним элементом.

[[nested-items]]
=== Вложенные элементы

Элементы `textItem` и `componentItem` поддерживают вложенные элементы, что позволяет определить иерархическое меню.

image::visual-components/components/user-menu-nested.png[align="center",width="330"]

[source,xml,indent=0]
----
include::example$/onboarding/src/main/resources/com/company/onboarding/view/component/usermenu/user-menu-view.xml[tags=user-menu-nested]
----

[[theme-variants]]
== Варианты темы

Используйте атрибут xref:vc/common-attributes.adoc#themeNames[themeNames] для настройки темы компонента.

[cols="1,4,1", options="header"]
|===
|Вариант
|Описание
|Поддерживается в

|`tertiary`
|Удаляет фон у основной кнопки.
|Aura, Lumo

|`non-checkable`
|Удаляет область, используемую для отображения флажка выбора.
|Aura, Lumo
|===

Тему `non-checkable` можно применять как к компоненту `userMenu`, так и к отдельным пунктам меню.

NOTE: Если подменю содержит элементы с флажками выбора, применяйте `non-checkable` отдельно к каждому элементу первого уровня.

[[renderers]]
== Рендереры

Фреймворк позволяет гибко настраивать содержимое кнопки и необязательного заголовка меню.

[[button-renderer]]
=== Рендерер кнопки

Рендерер кнопки можно задать либо методом `setButtonRenderer()`, либо аннотацией `@Install`.

image::visual-components/components/user-menu-button-renderer.png[align="center",width="196"]

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/component/usermenu/UserMenuView.java[tags=import-ui-components;import-file-storage;import-component;import-autowired;import-user-details;import-user;import-avatar;import-avatar-variant;import-span;import-div;import-file-ref;import-download-handler;import-download-response;some-code;user-menu-renderer-injects;user-menu-button-renderer;user-menu-renderer-helpers]
----
<1> Стандартные отступы `HorizontalLayout` слишком велики, поэтому мы добавляем нужные отступы с помощью стилей.
<2> Экземпляр компонента `Avatar` создается с помощью фабрики `UiComponents`.
<3> Полное имя пользователя передается в `Avatar`, чтобы сгенерировать резервные инициалы, если изображение недоступно.
<4> `avatar` по умолчанию получает фокус, но не предоставляет Java API для его отключения. Удалите `tabindex`, чтобы фокус мог получать только компонент `userMenu`.
<5> `DownloadHandler` передает изображение аватара из файлового хранилища по ссылке, сохраненной в атрибуте `picture` сущности `User`.

[[header-renderer]]
=== Рендерер заголовка

Рендерер заголовка определяет содержимое, отображаемое в верхней части меню. Его можно задать либо методом `setHeaderRenderer()`, либо аннотацией `@Install`.

image::visual-components/components/user-menu-header-renderer.png[align="center",width="234"]

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/component/usermenu/UserMenuView.java[tags=import-ui-components;import-file-storage;import-component;import-autowired;import-user-details;import-user;import-avatar;import-avatar-variant;import-span;import-div;import-file-ref;import-download-handler;import-download-response;some-code;user-menu-renderer-injects;user-menu-header-renderer;user-menu-renderer-helpers]
----
<1> Позиции компонентов задаются с помощью стилей.
<2> Экземпляр компонента `Avatar` создается с помощью фабрики `UiComponents`.
<3> Полное имя пользователя передается в `Avatar`, чтобы сгенерировать резервные инициалы, если изображение недоступно.
<4> `avatar` по умолчанию получает фокус, но не предоставляет Java API для его отключения. Удалите `tabindex`, чтобы фокус мог получать только компонент `userMenu`.
<5> `DownloadHandler` передает изображение аватара из файлового хранилища по ссылке, сохраненной в атрибуте `picture` сущности `User`.

[source,css,indent=0]
----
include::example$/onboarding/frontend/themes/onboarding/vc/user-menu.css[]
----

== Атрибуты

В Jmix существует множество xref:vc/common-attributes.adoc[общих атрибутов], которые выполняют одну и ту же функцию для всех компонентов.
Ниже приведены атрибуты, специфичные для `userMenu`:

[cols="1h,5,1",grid=rows,frame=none]
|===
|*Название*
|*Описание*
|*Значение по умолчанию*
|[[openOnHover]]openOnHover
|Если атрибут `openOnHover` установлен в `true`, выпадающий список элементов будет открываться автоматически, когда поле получает фокус с помощью мыши или касания.
|`false`
|===

== Обработчики

В Jmix существует множество xref:vc/common-handlers.adoc[общих обработчиков], которые настраиваются одинаково для всех компонентов.
Ниже приведены обработчики, специфичные для `userMenu`:

include::../handler-generation-tip.adoc[]

[cols="1h,6",grid=rows,frame=none]
|===
|*Название*
|*Описание*
|[[UserChangedEvent]]UserChangedEvent
|Событие `io.jmix.flowui.kit.component.usermenu.JmixUserMenu.UserChangedEvent` возникает, когда меняется пользователь, связанный с компонентом пользовательского меню.
|[[buttonRenderer]]buttonRenderer
|Устанавливает `Renderer`, отвечающий за отображение содержимого кнопки на основе текущего пользователя. Смотрите <<button-renderer,Рендерер кнопки>>.
|[[headerRenderer]]headerRenderer
|Устанавливает `Renderer`, отвечающий за отображение содержимого заголовка на основе текущего пользователя. Смотрите <<header-renderer,Рендерер заголовка>>.
|===
