= chart
:page-aliases: charts:chart-component.adoc

[NOTE.addon-component]
====
This component requires the free xref:charts:index.adoc#installation[Charts add-on].
====

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/charts" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

`chart` visualizes data using Apache ECharts.

[cols="1h,1", options="autowidth", grid=none, frame=none]
|===
|XML Element
|`chart`

|Java Class
|`Chart`
|===

[[basics]]
== Basics

Declare the `charts` namespace in the view's XML descriptor:

[source,xml]
----
<view xmlns="http://jmix.io/schema/flowui/view"
      xmlns:charts="http://jmix.io/schema/charts/ui"/>
----

[TIP]
====
Studio adds the namespace automatically when you add the component using the *Add Component* action in the top actions panel. See xref:studio:view-designer.adoc#component-palette[Component Palette].
====

Then declare and configure the chart using nested XML elements and attributes:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=chart]
----

After that, additional properties are set programmatically in Java:

[source,java,indent=0]
----
include::charts:example$/charts-ex1/src/main/java/com/company/demo/view/vehiclecount/VehicleCountChartView.java[tag=configure]
----

The resulting chart looks as follows:

image::charts:chart-component-1.png[align="center", width="675"]

[TIP]
====
When configuring a chart, you can refer to the https://echarts.apache.org/en/option.html#title[official Apache ECharts documentation^] to find the properties you need, and then use corresponding Jmix Chart properties. Most of the time, you will find exact matches between JavaScript and Jmix APIs.
====

[[data-set]]
== Data Set

++++
<div class="jmix-ui-live-demo-container">
<a href="https://echarts.apache.org/en/option.html#dataset" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `dataSet` property defines the data source for the chart's <<series,series>> and enables data reuse across multiple `series` within one `charts` tag.

In the following example, a data set is configured in XML and connects a chart to the `vehiclesDc` data container:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=dataSet]
----

The chart will use the `year` attribute of the entity to get a category name and `cars`, `motorcycles` and `bicycles` attributes to get values.

The same data set can be configured in Java as follows:

[source,java,indent=0]
----
include::charts:example$/charts-ex1/src/main/java/com/company/demo/view/vehiclecount/VehicleCountChartView.java[tag=dataSet]
----

NOTE: The `chart` element can include only one `dataSet` configuration.

[[series]]
== Series

++++
<div class="jmix-ui-live-demo-container">
<a href="https://echarts.apache.org/en/option.html#series" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `series` element defines the type of chart to display (e.g., line, bar, scatter). Each chart type can be configured differently within the `series` element.

The basic chart types supported by Jmix include:

* https://demo.jmix.io/ui-samples/sample/line-series-simple[Line series^]
* https://demo.jmix.io/ui-samples/sample/bar-series-simple[Bar series^]
* https://demo.jmix.io/ui-samples/sample/pie-series-simple[Pie series^]
* https://demo.jmix.io/ui-samples/sample/scatter-series-simple[Scatter^] and https://demo.jmix.io/ui-samples/sample/effect-scatter-series[effect-scatter series^]
* https://demo.jmix.io/ui-samples/sample/candlestick-series-simple[Candlestick series^]
* https://demo.jmix.io/ui-samples/sample/funnel-series-simple[Funnel series^]
* https://demo.jmix.io/ui-samples/sample/gauge-series-simple[Gauge series^]
* https://demo.jmix.io/ui-samples/sample/radar-series-simple[Radar series^]

In the following example, the `series` element defines a pie chart:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=series-pie]
----

The `label` element configures text labels of pie segments. Its `formatter` attribute defines a pattern which can include the data item name and value. See more about https://echarts.apache.org/en/option.html#series-pie.label[label^] and https://echarts.apache.org/en/option.html#series-pie.label.formatter[formatter^] in the ECharts documentation.

The resulting pie chart looks as follows:

image::charts:chart-component-series-1.png[align="center", width="507"]


NOTE: The `chart` element can include multiple `series` configurations, in which case the data will be shared across all chart types.

[[title]]
== Title

++++
<div class="jmix-ui-live-demo-container">
<a href="https://echarts.apache.org/en/option.html#title" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `title` property displays the chart's main title and subtitle, customizable in text content, positioning, alignment, and style.

For example:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=title]
----

This configuration specifies both the main title and a subtitle for the chart, positioned in the top right corner with centered text alignment and italic text style:

image::charts:chart-component-title-1.png[align="center", width="608"]

[[legend]]
== Legend

++++
<div class="jmix-ui-live-demo-container">
<a href="https://echarts.apache.org/en/option.html#legend" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `legend` property shows the symbol, color, and name of different series. Legends can be clicked to toggle the display of the chart series. The legend is automatically generated based on the provided data.

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=legend]
----

This example displays a legend with symbols on the right:

image::charts:chart-component-legend-1.png[align="center", width="608"]

[[data-zoom]]
== Data Zoom

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://echarts.apache.org/en/option.html#dataZoom" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `dataZoom` property provides two primary features:

* *Inside data zoom* enables users to zoom or roam within coordinate systems through mouse dragging, mouse wheel movements, or finger touch gestures on touch screens.

* *Slider type data zoom* offers features like data thumbnails for a quick overview, zooming in/out, selection brushing, drag to move, and click to locate within the chart's data range.

For example:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=dataZoom]
----

Notice the horizontal and vertical sliders displayed on the chart:

image::charts:chart-component-data-zoom-1.png[align="center", width="613"]

[[native-json]]
== Native JSON

The `nativeJson` property allows you to configure the chart using a direct JSON string as an alternative when the component's Java API or XML attributes do not meet specific customization requirements. This feature provides access to the full range of ECharts' configuration options.

For example:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=nativeJson]
----

Here the `nativeJson` element configures the chart title in the same way as in the XML configuration <<title,above>>.

NOTE: `nativeJson` configuration is merged with existing properties and overrides corresponding properties defined in XML.

[[tool-box]]
== Toolbox

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://echarts.apache.org/en/option.html#toolbox" class="echarts-docs-btn" target="_blank">ECharts</a>
</div>
++++

The `toolbox` property provides a group of utility tools, which includes https://echarts.apache.org/en/option.html#toolbox.feature.saveAsImage[export^], https://echarts.apache.org/en/option.html#toolbox.feature.dataView[data view^], https://echarts.apache.org/en/option.html#toolbox.feature.magicType[dynamic type switching^], https://echarts.apache.org/en/option.html#toolbox.feature.dataZoom[data area zooming^], and https://echarts.apache.org/en/option.html#toolbox.feature.restore[reset configurations^].

For example, the following configuration adds ability to export the chart as a PNG image:

[source,xml,indent=0]
----
include::charts:example$charts-ex1/src/main/resources/com/company/demo/view/vehiclecount/vehicle-count-chart-view.xml[tags=toolBox]
----

Notice the download icon in the top right corner of the chart:

image::charts:chart-component-export-1.png[align="center", width="661"]

== Attributes

The following attributes are specific to `chart`:

[cols="2,5,1", options="header"]
|===
|Name
|Description
|Default

|[[animation]]`animation`
|Enables or disables chart animation.
|`true`

|[[animationDelay]]`animationDelay`
|Sets the delay before the initial animation, in milliseconds.
|—

|[[animationDelayUpdate]]`animationDelayUpdate`
|Sets the delay before an update animation, in milliseconds.
|—

|[[animationDuration]]`animationDuration`
|Sets the duration of the initial animation, in milliseconds.
|—

|[[animationDurationUpdate]]`animationDurationUpdate`
|Sets the duration of an update animation, in milliseconds.
|—

|[[animationEasing]]`animationEasing`
|Sets the easing function for the initial animation.
|—

|[[animationEasingUpdate]]`animationEasingUpdate`
|Sets the easing function for an update animation.
|—

|[[animationThreshold]]`animationThreshold`
|Sets the maximum number of graphic elements for which animation remains enabled.
|—

|[[backgroundColor]]`backgroundColor`
|Sets the chart background color as a CSS color.
|—

|[[blendMode]]`blendMode`
|Sets the global blend mode. Possible values are `SOURCE_OVER`, `SOURCE_IN`, `SOURCE_OUT`, `SOURCE_ATOP`, `DESTINATION_OVER`, `DESTINATION_IN`, `DESTINATION_OUT`, `DESTINATION_ATOP`, `LIGHTER`, `COPY`, `XOR`, `MULTIPLY`, `SCREEN`, `OVERLAY`, `DARKEN`, `LIGHTEN`, `COLOR_DODGE`, `COLOR_BURN`, `HARD_LIGHT`, `SOFT_LIGHT`, `DIFFERENCE`, `EXCLUSION`, `HUE`, `SATURATION`, `COLOR`, and `LUMINOSITY`.
|`SOURCE_OVER`

|[[colorPalette]]`colorPalette`
|Sets a comma-separated palette of CSS colors used by the chart series.
|—

|[[hoverLayerThreshold]]`hoverLayerThreshold`
|Sets the element-count threshold above which hovered elements are rendered on a separate layer.
|—

|[[renderer]]`renderer`
|Selects the client-side renderer. Possible values are `CANVAS` and `SVG`.
|`CANVAS`

|[[useUtc]]`useUtc`
|Sets whether ECharts should parse and display time values in UTC.
|—
|===

The following shared attributes are supported by `chart`:

xref:flow-ui:vc/common-attributes.adoc#id[id] -
xref:flow-ui:vc/common-attributes.adoc#alignSelf[alignSelf] -
xref:flow-ui:vc/common-attributes.adoc#classNames[classNames] -
xref:flow-ui:vc/common-attributes.adoc#colspan[colspan] -
xref:flow-ui:vc/common-attributes.adoc#css[css] -
xref:flow-ui:vc/common-attributes.adoc#height[height] -
xref:flow-ui:vc/common-attributes.adoc#maxHeight[maxHeight] -
xref:flow-ui:vc/common-attributes.adoc#maxWidth[maxWidth] -
xref:flow-ui:vc/common-attributes.adoc#minHeight[minHeight] -
xref:flow-ui:vc/common-attributes.adoc#minWidth[minWidth] -
xref:flow-ui:vc/common-attributes.adoc#visible[visible] -
xref:flow-ui:vc/common-attributes.adoc#width[width]

== Handlers

The following events are specific to `chart`:

include::flow-ui:vc/handler-generation-tip.adoc[]

[cols="2,5", options="header"]
|===
|Name
|Description

|[[ChartClickEvent]]`ChartClickEvent`
|Fired when the user clicks a chart element.

|[[ChartDataZoomEvent]]`ChartDataZoomEvent`
|Fired when a data zoom operation changes the visible data range.

|[[ChartDoubleClickEvent]]`ChartDoubleClickEvent`
|Fired when the user double-clicks a chart element.

|[[ChartFinishedEvent]]`ChartFinishedEvent`
|Fired when rendering and all animations have finished.

|[[ChartGlobalCursorTakenEvent]]`ChartGlobalCursorTakenEvent`
|Fired when a toolbox interaction takes control of the global cursor.

|[[ChartGlobalOutEvent]]`ChartGlobalOutEvent`
|Fired when the pointer leaves the chart area.

|[[ChartLegendInverseSelectEvent]]`ChartLegendInverseSelectEvent`
|Fired when the legend selection is inverted.

|[[ChartLegendScrollEvent]]`ChartLegendScrollEvent`
|Fired when a scrollable legend changes page.

|[[ChartLegendSelectAllEvent]]`ChartLegendSelectAllEvent`
|Fired when all legend items are selected.

|[[ChartLegendSelectChangedEvent]]`ChartLegendSelectChangedEvent`
|Fired when a legend item changes its selected state.

|[[ChartMagicTypeChangedEvent]]`ChartMagicTypeChangedEvent`
|Fired when the toolbox changes the chart series type.

|[[ChartMouseDownEvent]]`ChartMouseDownEvent`
|Fired when a pointer button is pressed over the chart.

|[[ChartMouseMoveEvent]]`ChartMouseMoveEvent`
|Fired when the pointer moves over the chart.

|[[ChartMouseOutEvent]]`ChartMouseOutEvent`
|Fired when the pointer leaves a chart element.

|[[ChartMouseOverEvent]]`ChartMouseOverEvent`
|Fired when the pointer enters a chart element.

|[[ChartMouseUpEvent]]`ChartMouseUpEvent`
|Fired when a pointer button is released over the chart.

|[[ChartRenderedEvent]]`ChartRenderedEvent`
|Fired after the chart has rendered or rerendered.

|[[ChartRestoreEvent]]`ChartRestoreEvent`
|Fired when the toolbox restore action resets the chart configuration.
|===

The following shared handlers are supported by `chart`:

xref:flow-ui:vc/common-handlers.adoc#AttachEvent[AttachEvent] -
xref:flow-ui:vc/common-handlers.adoc#DetachEvent[DetachEvent]

== Elements

A `chart` can include `title`, `legend`, `scrollableLegend`, `tooltip`, `toolbox`, `brush`, `grid`, `aria`, `xAxes`, `yAxes`, `polar`, `radiusAxis`, `angleAxis`, `radar`, `dataZoom`, `visualMap`, `axisPointer`, `dataSet`, `series`, `textStyle`, `stateAnimation`, and `nativeJson` as its nested elements. These elements correspond to the options of the Apache ECharts configuration model.

== See Also

* xref:charts:index.adoc[]
* xref:charts:data-binding.adoc[]
* https://echarts.apache.org/en/option.html[Apache ECharts option documentation^]
