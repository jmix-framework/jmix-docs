= Dialogs

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/dialogs-and-notifications" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

The `Dialogs` interface is designed to display standard dialog windows. A dialog window is a small popup window that you can use to present information and user interface elements in an overlay.

The `createMessageDialog()`, `createOptionDialog()`, and `createInputDialog()` methods are the entry points to the fluent API that allows you to construct and show dialogs.

[[message-dialog]]
== Message Dialog

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/message-dialog" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

A message dialog shows a message to the user.

image::dialogs/message-dialog.png[align="center", width = 399]

In the following example, a message dialog is shown when the user clicks a button:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;messageDialog-1]
----

<1> Adds a dialog header.
<2> Adds a text message to show in the dialog.

The following methods allow you to customize the look and behavior of the message dialog:

<<with-header,withHeader>> -
<<with-width,withWidth>> -
<<with-height,withHeight>> -
<<with-left,withLeft>> -
<<with-top,withTop>> -
<<with-text,withText>> -
<<with-content,withContent>> -
<<with-modal,withModal>> -
<<with-themeName,withThemeName>> -
<<with-className,withClassName>> -
<<with-draggable,withDraggable>> -
<<with-resizable,withResizable>> -
<<with-min-max-sizes,withMinWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxHeight>> -
<<with-closeOnOutsideClick,withCloseOnOutsideClick>> -
<<with-closeOnEsc,withCloseOnEsc>> -
<<with-draggedListener,withDraggedListener>> -
<<with-resizeListener,withResizeListener>>

[[option-dialog]]
== Option Dialog

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/option-dialog" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

The option dialog displays a message and a set of buttons for user reaction.

image::dialogs/option-dialog.png[align="center", width = 400]

Use the `withActions()` method to provide xref:actions.adoc[actions], each of which is represented by a button in the dialog. For example:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;optionsDialog-1]
----

<1> If you click Yes, the dialog closes and invokes the corresponding `addCustomer()` action method.

The `DialogAction` base class is designed to create actions with standard names and icons. Five types of actions, defined by the `DialogAction.Type` enum, are supported: `OK`, `CANCEL`, `YES`, `NO`, `CLOSE`. Names of buttons are extracted from the xref:localization:message-bundles.adoc[message bundle].

The following methods allow you to customize the look and behavior of the option dialog:

<<with-header,withHeader>> -
<<with-width,withWidth>> -
<<with-height,withHeight>> -
<<with-left,withLeft>> -
<<with-top,withTop>> -
<<with-text,withText>> -
<<with-content,withContent>> -
<<with-themeName,withThemeName>> -
<<with-className,withClassName>> -
<<with-draggable,withDraggable>> -
<<with-resizable,withResizable>> -
<<with-min-max-sizes,withMinWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxHeight>> -
<<with-draggedListener,withDraggedListener>> -
<<with-resizeListener,withResizeListener>>

[[background-task-dialog]]
== Background Task Dialog

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/background-task-dialog" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Background task dialog provides a user-friendly experience for long-running tasks, where the user can monitor progress and cancel the operation if needed.

TIP: For more information on the background tasks mechanism, please see the xref:background-tasks.adoc[Background Tasks] article.

image::dialogs/backgroundtask-dialog.png[align="center", width = 402]

To configure a dialog, provide it with a background task object:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;backgroundTaskDialog]
----

<1> Pass a background task object to the dialog.
<2> Set the number of progress bar sections based on the number of tasks.
<3> Allow user to interrupt the task.

The following methods allow you to customize the look and behavior of the background task dialog:

<<with-header,withHeader>> -
<<with-width,withWidth>> -
<<with-height,withHeight>> -
<<with-left,withLeft>> -
<<with-top,withTop>> -
<<with-text,withText>> -
<<with-content,withContent>> -
<<with-themeName,withThemeName>> -
<<with-className,withClassName>> -
<<with-draggable,withDraggable>> -
<<with-resizable,withResizable>> -
<<with-min-max-sizes,withMinWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxWidth>> -
<<with-min-max-sizes,withMaxHeight>> -
<<with-draggedListener,withDraggedListener>> -
<<with-resizeListener,withResizeListener>>

[[input-dialog]]
== Input Dialog

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/input-dialog" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

Input dialog is a versatile tool that allows you to construct input forms using API and often saves you from creating screens for trivial data input. It enables entering values of different types, validates the input, and provides different actions to be selected by the user.

image::dialogs/input-dialog.png[align="center", width = 560]

The following methods allow you to customize the look and behavior of the input dialog:

<<with-header,withHeader>> -
<<with-width,withWidth>> -
<<with-height,withHeight>> -
<<with-left,withLeft>> -
<<with-top,withTop>> -
<<with-text,withText>> -
<<with-draggable,withDraggable>> -
<<with-draggedListener,withDraggedListener>>

Let's consider some examples.

[[standard-parameters]]
=== Standard Parameters

Use the `withParameters()` method to add parameters, each of which will be represented by an input field in the dialog.
The following example creates an input dialog with parameters of standard types and standard `OK/Cancel` actions:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;inputDialog-standard-parameters]
----

<1> Specifies a mandatory string parameter.
<2> Specifies an integer parameter with the default value.
<3> Specifies an entity parameter.
<4> Specifies an enumeration parameter.
<5> Specifies standard `OK/Cancel` actions represented by buttons at the bottom of the dialog.
<6> In the close listener, we can check what action was used by the user.
<7> The close event contains entered values that can be obtained using parameter identifiers.

[[custom-parameters]]
=== Custom Parameters

The following example illustrates creating a custom parameter that lets the user select a value from a combobox:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;inputDialog-custom-parameters]
----

<1> Specifies a custom parameter.
<2> Creates a combobox within a custom parameter field.
<3> Loads a list of options into the combobox.

[NOTE]
====
When using the `InputParameter.withField()` method, only the `label` attribute from the `InputParameter` is propagated to the created field. Other attributes set on the `InputParameter` are *not* automatically applied to the field created inside the `withField()` lambda.

The following `InputParameter` attributes are *not* propagated when using `withField()`:

- `required`
- `requiredMessage`
- `datatype`
- `defaultValue`

This is because you are fully responsible for manually creating and configuring the field inside the lambda. Therefore, all additional settings except for the `label` must be explicitly applied.

In the example below, important attributes like `datatype` and `required` need to be set manually on the created field. If you rely solely on `withField()` without explicitly configuring these attributes, they will not be applied automatically.

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=required-custom-parameter]
----

If you do not need to customize the field manually, you can simply configure the attributes directly on the `InputParameter`:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=required-custom-parameter-2]
----

In this case, Jmix will automatically create the field and apply all specified attributes.
====

[[custom-validator]]
=== Custom Validator

The dialog enables some basic validation from the start: it can validate the type of entered values or check that the required field is not empty. On top of it you can add more general, custom validators.

The following example adds a validator checking that at least one parameter is entered:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;inputDialog-custom-validator]
----

<1> The custom validator adding logic to ensure at least one parameter is entered.
<2> In the validator, parameter values can be obtained from the context object.
<3> The validator returns validation errors if no parameters are entered.

[[side-dialog]]
== Side Dialog

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/side-dialog" class="live-demo-btn" target="_blank">LIVE DEMO</a>
</div>
++++

A dialog that slides in from one side of the view.

image::dialogs/side-dialog.png[align="center", width = 1155]

The dialog can be opened using the `Dialogs` bean:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;inject-message-bundle;side-dialog]
----

=== Position and Size

Use `withSideDialogPosition()` to define where the dialog appears. Possible values are `RIGHT`, `TOP`, `BOTTOM`, `LEFT`, `INLINE_START`, and `INLINE_END`. The default value is `RIGHT`. `INLINE_START` and `INLINE_END` follow the text direction (LTR/RTL).

The dialog size is configured differently depending on its orientation.

For horizontal dialogs (`LEFT`, `RIGHT`, `INLINE_START`, `INLINE_END`):

* `setHorizontalSize()`
* `setHorizontalMaxSize()`
* `setHorizontalMinSize()`

For vertical dialogs (`TOP`, `BOTTOM`):

* `setVerticalSize()`
* `setVerticalMaxSize()`
* `setVerticalMinSize()`

=== Content

You can define dialog content using providers:

[cols="1h,4", grid=rows]
|===
|Method |Description
|`withContentComponents()`
|Provides components for the dialog content area.
|`withHeaderProvider()`
|Provides custom components for the header.
|`withFooterProvider()`
|Provides custom components for the footer.
|===

=== Events

The following events are available:

[cols="1h,4", grid=rows]
|===
|Event |Description
|`SideDialogOpenedChangeEvent`
|Fired when the dialog is opened or closed.
|`SideDialogCloseActionEvent`
|Fired when the user presses `Esc` or clicks outside the dialog. You can use this event to control whether the dialog should close.
|===

=== Styling

Side dialog appearance and sizing can be customized using CSS.

==== Sizing

You can configure dialog size globally using CSS variables. These values are applied to all `SideDialog` instances.

NOTE: If sizes are set programmatically with Java API, they take precedence over CSS variables.

Horizontal sizing (for `LEFT`, `RIGHT`, `INLINE_START`, `INLINE_END`):

[cols="2h,3,1", grid=rows]
|===
|Variable |Description |Default
|`--jmix-side-dialog-horizontal-size`
|Base horizontal size of the dialog.
|`auto`
|`--jmix-side-dialog-horizontal-max-size`
|Maximum horizontal size.
|`50%`
|`--jmix-side-dialog-horizontal-min-size`
|Minimum horizontal size.
|`16em`
|===

Vertical sizing (for `TOP`, `BOTTOM`):

[cols="2h,3,1", grid=rows]
|===
|Variable |Description |Default
|`--jmix-side-dialog-vertical-size`
|Base vertical size of the dialog.
|`auto`
|`--jmix-side-dialog-vertical-max-size`
|Maximum vertical size.
|`50vh`
|`--jmix-side-dialog-vertical-min-size`
|Minimum vertical size.
|`16em`
|===

==== Appearance

[cols="2h,3,1", grid=rows]
|===
|Variable |Description |Default
|`--jmix-side-dialog-transition-duration`
|Animation duration of dialog opening and closing.
|`200ms`
|===

==== Parts

The side dialog exposes the following xref:themes/styling-ui-components.adoc#component-parts[stylable parts]:

[cols="2h,3", grid=rows]
|===
|Part |Description
|`jmix-side-dialog-overlay::part(backdrop)`
|Modality curtain (background overlay behind the dialog).
|`jmix-side-dialog-overlay::part(overlay)`
|Dialog overlay (main container).
|`jmix-side-dialog-overlay::part(header)`
|Header section of the dialog.
|`jmix-side-dialog-overlay::part(title)`
|Title wrapper element.
|`jmix-side-dialog-overlay > [slot="title"]`
|Title element.
|`jmix-side-dialog-overlay::part(header-content)`
|Container for custom header content.
|`jmix-side-dialog-overlay::part(content)`
|Main content area.
|`jmix-side-dialog-overlay::part(footer)`
|Footer section.
|===

==== States

The side dialog exposes the following xref:themes/styling-ui-components.adoc#component-states[states]:

[cols="2h,3", grid=rows]
|===
|State |Description
|`jmix-side-dialog-overlay[side-dialog-position='right']`
|Applied according to dialog position, for example `left`, `top`, or `bottom`.
|`jmix-side-dialog-overlay[fullscreen]`
|Applied when the dialog is displayed in fullscreen mode on small devices.
|===

[[dialog-configuration]]
== Dialog Configuration

You can customize the header, size, and position of all dialogs using the following methods:

[[with-header]]
* `withHeader()` - sets the header text.

[[with-width]]
* `withWidth()` - sets the dialog width.

[[with-height]]
* `withHeight()` - sets the dialog height.

[[with-left]]
* `withLeft()` - sets the dialog's left offset from its container (unitless values are interpreted as pixels)

[[with-top]]
* `withTop()` - sets the dialog's top offset (unitless values are interpreted as pixels)

For example:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=basic-config]
----

Additional appearance and behavior configurations are available for specific dialog types:

[[with-text]]
* `withText()` - sets the text message to be displayed in the dialog.

[[with-content]]
* `withContent()` - sets the dialog's content. This is text rendered within a `Paragraph` component. Note that this overrides any text previously set via `withText()`.
+
[NOTE]
====
Text formatting is available with HTML - the dialog can display HTML content using the `withContent()` method. A given HTML fragment must be encapsulated in a component:

[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=inject-dialogs;messageDialog-html]
----
====

[[with-modal]]
* `withModal()` - when set to `false`, displays the dialog as non-modal, allowing interaction with other application components. Dialogs are modal by default.

[[with-themeName]]
* `withThemeName()` - sets the dialog's theme name(s), replacing any previously set values.

[[with-className]]
* `withClassName()` - sets the CSS class name(s) for the component, replacing all previously defined classes.

[[with-draggable]]
* `withDraggable()` - sets whether dialog is enabled to be dragged by the user or not. Dialogs are draggable by default.

[[with-resizable]]
* `withResizable()` - sets whether dialog can be resized by user or not.
By default, the dialog is not resizable.

[[with-min-max-sizes]]
* `withMinWidth()`, `withMaxWidth()`, `withMaxWidth()`, `withMaxHeight()` - set the dialog's minimum/maximum width and height dimensions respectively.

[[with-closeOnOutsideClick]]
* `withCloseOnOutsideClick()` - controls whether clicking outside the dialog closes it. By default, the dialog is closable with an outside click.

[[with-closeOnEsc]]
* `withCloseOnEsc()` - controls whether pressing `ESC` closes the dialog. By default, the dialog is closable with `ESC`.

[[with-draggedListener]]
* `withDraggedListener()` - registers a callback invoked after user dragging completes. It is called only if dragging is enabled.
+
NOTE: By default, the component will sync the top/left values after every dragging.
+
For example:
+
[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=withDraggedListener]
----

[[with-resizeListener]]
* `withResizeListener()` - registers a callback invoked after user resizing completes. It is called only if resizing is enabled.
+
NOTE: By default, the component will sync the width/height and top/left values after every resizing.
+
For example:
+
[source,java,indent=0]
----
include::example$/onboarding/src/main/java/com/company/onboarding/view/dialogsandnotifications/dialogs/DialogsSampleView.java[tags=withResizeListener]
----
