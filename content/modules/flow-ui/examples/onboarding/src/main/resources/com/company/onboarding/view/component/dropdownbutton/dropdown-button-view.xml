<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="msg://dropdownButtonView.title">
    <!-- tag::actions[] -->
    <actions>
        <action id="callAction" text="Call Us" icon="PHONE"/>
    </actions>
    <!-- end::actions[] -->
    <!-- tag::layout[] -->
    <layout>
        <!-- end::layout[] -->
        <hbox>
            <dropdownButton text="Contact Us" openOnHover="true">
            <items>
                <textItem id="call" text="Call Us"/>
                <textItem id="email" text="E Mail"/>
                <textItem id="whatsApp" text="WhatsApp"/>
            </items>
        </dropdownButton>
            <!-- tag::basics[] -->
        <dropdownButton icon="MAILBOX" text="Contact Us" title="msg://contact">
            <items>
                <textItem id="callItem" text="Call Us"/>
                <textItem id="emailItem" text="E Mail"/>
                <textItem id="whatsAppItem" text="WhatsApp"/>
            </items>
        </dropdownButton>
            <!-- end::basics[] -->
            <!-- tag::dropdownButton[] -->
        <dropdownButton icon="MAILBOX" title="msg://contact" id="callBtn">
            <items>
                <!-- end::dropdownButton[] -->
                <!-- tag::actionItem[] -->
                <actionItem id="callUsItem" ref="callAction"/>
                <!-- end::actionItem[] -->
                <!-- tag::componentItem[] -->
                <componentItem id="emailIt">
                    <hbox padding="false">
                        <icon icon="MAILBOX"/>
                        <span text="E Mail"/>
                    </hbox>
                </componentItem>
                <!-- end::componentItem[] -->
                <separator>
                </separator>
                <!-- tag::textItem[] -->
                <textItem id="whatsAppIt" text="WhatsApp"/>
                <!-- end::textItem[] -->
                <!-- tag::dropdownButton[] -->
            </items>
        </dropdownButton>
            <!-- end::dropdownButton[] -->
        </hbox>
        <!-- tag::layout[] -->
    </layout>
    <!-- end::layout[] -->
</view>