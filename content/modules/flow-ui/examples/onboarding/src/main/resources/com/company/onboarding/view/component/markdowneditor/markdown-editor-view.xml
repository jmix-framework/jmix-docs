<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="msg://markdownEditorView.title">
    <data>
        <instance id="orderDc" class="com.company.onboarding.entity.Order">
            <loader id="orderDl"/>
            <fetchPlan extends="_base"/>
        </instance>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
    </facets>
    <layout>
        <!-- tag::markdown-editor[] -->
        <markdownEditor id="markdownEditor"
                        themeNames="toolbar-align-end"
                        label="Markdown Editor"
                        helperText="Markdown is supported. Use the toolbar or keyboard shortcuts to format text."
                        placeholder="Write your content here..."/>
        <!-- end::markdown-editor[] -->
        <!-- tag::markdown-editor-data-binding[] -->
        <markdownEditor id="descriptionField"
                        dataContainer="orderDc"
                        property="description"
                        label="Description"/>
        <!-- end::markdown-editor-data-binding[] -->
        <!-- tag::markdown-editor-mode[] -->
        <markdownEditor id="markdownEditorField"
                        mode="PREVIEW"/>
        <!-- end::markdown-editor-mode[] -->
        <!-- tag::markdown-editor-theme-names[] -->
        <markdownEditor id="markdownEdit"
                        themeNames="helper-above-field toolbar-align-end"/>
        <!-- end::markdown-editor-theme-names[] -->
        <!-- tag::markdown-editor-css[] -->
        <markdownEditor id="markdownEditCss"
                        css="--jmix-markdown-editor-default-width: 32rem;
                             --jmix-markdown-editor-header-background: var(--lumo-contrast-5pct);"/>
        <!-- end::markdown-editor-css[] -->
    </layout>
</view>