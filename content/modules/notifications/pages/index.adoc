= 通知

该扩展组件支持创建消息并通过各种消息通道给用户发送通知。用户可以接收信息类通知和系统事件通知。

// as well as notifications from business processes.

[[installation]]
== 安装

按照 xref:ROOT:add-ons.adoc[扩展组件] 章节的说明进行安装。

NOTE: 该扩展组件需要企业版 https://www.jmix.io/subscription-plans-and-prices/[订阅^]。

也可以通过配置 `build.gradle` 文件手动安装：

. 配置 premium 仓库：
+
[source,groovy]
----
repositories {
    // ...
    maven {
        url = 'https://global.repo.jmix.io/repository/premium'
        credentials {
            username = rootProject['premiumRepoUser']
            password = rootProject['premiumRepoPass']
        }
    }
}
----

. 添加依赖：
+
[source,groovy,indent=0]
----
include::notifications:example$ex1/build.gradle[tags=dependencies]
----
