= Multitenancy

Дополнение позволяет создавать мультитенантные приложения Jmix, в которых данные нескольких тенантов хранятся в одной базе данных. Один экземпляр приложения может обслуживать несколько тенантов, определяемых как группы пользователей, изолированные друг от друга и имеющие доступ только к определенным (часто только для чтения) данным.

В мультитенантном приложении Jmix существует две основные категории данных:

* *Общие данные*:
** Данные, которые используются совместно всеми тенантами в приложении.
** Тенанты имеют доступ только для чтения к этим общим данным, которые являются общедоступными, но не могут изменяться отдельными тенантами.

* *Данные конкретного тенанта*:
** Данные, специфичные для каждого тенанта, которые не видны и недоступны другим тенантам.
** Тенанты имеют полный доступ к своим собственным данным, позволяя им взаимодействовать и изменять эти данные по мере необходимости, не затрагивая данные, принадлежащие другим тенантам.

Реализуя мультитенантность в приложениях Jmix, разработчики могут эффективно управлять сервисами и предоставлять их множеству клиентов или групп пользователей, обеспечивая изоляцию данных, безопасность и персонализированный контроль доступа в соответствии с конкретными потребностями каждого тенанта. Этот подход оптимизирует организацию данных и доступ к ним, сохраняя конфиденциальность и целостность данных в различных группах пользователей.

CAUTION: Дополнение xref:rest:index.adoc[Generic REST] не полностью поддерживает мультитенантность. Оно не разделяет экземпляры сущностей по тенантам.

[[installation]]
== Установка

Для автоматической установки через Jmix Marketplace следуйте инструкциям в разделе xref:ROOT:add-ons.adoc#installation[Дополнения].

Для ручной установки добавьте следующие зависимости в ваш `build.gradle`:

[source,groovy,indent=0]
----
include::example$/multitenancy-ex1/build.gradle[tags=dependency]
----

include::ROOT:single-menu-note.adoc[]

[[how-it-works]]
== Как это работает

В вашем проекте специфичные для тенанта сущности должны включать строковый атрибут, аннотированный `@TenantId`. Когда пользователь тенанта загружает эти сущности, фреймворк применяет условие `WHERE` на основе атрибута `tenant-id` к JPQL-запросу, чтобы выбрать только данные, относящиеся к тенанту пользователя. Кроме того, атрибут `tenant-id` автоматически присваивается тенанту текущего пользователя при сохранении новых сущностей.

Вы можете добавить этот атрибут, используя черту xref:data-model:entities.adoc#tenant-id-trait[Has Tenant ID] в xref:studio:entity-designer.adoc[дизайнере сущностей] Studio.

CAUTION: Автоматическая фильтрация для нативного SQL отсутствует, поэтому пользователи тенанта не должны иметь доступ к любым функциям, предоставляющим доступ к нативному SQL или коду Groovy (xref:jmx-console:index.adoc[JMX Console], создание xref:reports:index.adoc[отчетов] и т.д.).

В вашем проекте сущность `User` должна включать атрибут `tenant-id`. Этому атрибуту должно быть присвоено определенное значение для всех пользователей тенанта. Пользователи без значения в этом атрибуте (те, кто не связан с каким-либо конкретным тенантом) могут получать доступ к данным всех тенантов. Такая настройка подходит для __глобальных администраторов__, которые отвечают за настройку тенантов и наблюдение за всей системой.

== Дополнения и модули с поддержкой Multitenancy

Следующие дополнения и модули корректно хранят информацию и обеспечивают изоляцию тенантов при использовании дополнения Multitenancy. Конкретные сущности, которые содержат атрибут `sysTenantId`, перечислены рядом с каждым дополнением.

* xref:audit:index.adoc[Audit]: `EntityLogItem`.
* xref:bpm:index.adoc[BPM]: `ContentStorage`, `UserGroup`, `UserGroupRole`, `UserGroupUser`.
* xref:email:index.adoc[Email Sending]: `SendingMessage`, `SendingAttachment`.
* xref:reports:index.adoc[Reports]: `Report`, `ReportGroup`.
* xref:security:index.adoc[Подсистема безопасности] (встроена в фреймворк): `ResourceRoleEntity`, `RowLevelRoleEntity`.
* Конфигурация фильтров (встроена в фреймворк): `FilterConfiguration`.

Чтобы оценить поддержку Multitenancy в кастомных дополнениях:

* Проверьте документацию дополнения на наличие упоминаний `sysTenantId` или `@TenantId`.
* Изучите сущности в дополнении, чтобы узнать, есть ли у них атрибут тенанта и как он используется.

[[managing-tenants]]
== Управление тенантами

Дополнение предоставляет экран *Multitenancy -> Tenants*, который позволяет глобальным администраторам создавать и изменять тенанты.

Сущность регистрации тенанта имеет два атрибута:

* *Tenant id* - идентификатор, используемый в специфичных для тенанта сущностях. Не может быть изменен после создания.
* *Tenant name* - описательное имя тенанта.

image::tenants-view.png[align="center", width="1070"]

[[tenant-users]]
== Пользователи тенанта

В мультитенантных приложениях пользователи в разных тенантах могут иметь одинаковые логины. Чтобы гарантировать уникальность атрибута `username` во всем приложении, пользователям тенанта следует регистрироваться с логином, включающим префикс `tenant-id`. Например, если есть два разных пользователя с именем `Alice` в тенантах `t1` и `t2`, их логины должны быть `t1|alice` и `t2|alice` соответственно.

Пользователи тенанта могут входить в приложение, указывая полный логин, включающий `tenant-id`, например, `t1|alice`.

Существует два способа входа пользователей аренды в приложение:

. Первый способ - использование параметра URL, который включает tenant-id при доступе к экрану входа, например `++http://localhost:8080/login?tenantId=t1++`. В этом случае пользователи могут просто ввести своё имя для входа без префикса tenant-id, например, просто `alice`.
+
У вас также есть возможность задать пользовательское имя для параметра URL с помощью свойства приложения `jmix.multitenancy.tenantIdUrlParamName application`.

. Пользователи могут указать полное имя пользователя, включая tenant-id, например, `t1|alice`.

NOTE: Вы можете реализовать собственную схему уникальных логинов вместо описанного выше метода.

[CAUTION]
====
*Замещение пользователей*

Пользователь, принадлежащий определённому тенанту, должен xref:security:users.adoc#user-substitution[замещаться] только пользователями из того же тенанта.
====

[[configuring-users]]
== Настройка пользователей

В этом разделе мы объясним процесс настройки управления пользователями и аутентификации в вашем проекте для поддержки мультитенантности.

. Добавьте строковый атрибут в вашу сущность `User` и аннотируйте его с помощью `@TenantId`. Вы также можете добавить этот атрибут, используя черту xref:data-model:entities.adoc#tenant-id-trait[Has Tenant ID] в Studio:
+
[source,java,indent=0]
----
include::example$/multitenancy-ex1/src/main/java/com/company/demo/entity/User.java[tags=tenantId]
----

. Добавьте колонку `tenant` в таблицу данных в `user-list-view.xml`:
+
[source,xml,indent=0]
----
include::example$/multitenancy-ex1/src/main/resources/com/company/demo/view/user/user-list-view.xml[tags=tenant]
----

. Добавьте поле для выбора тенанта в `user-detail-view.xml`:
+
[source,xml,indent=0]
----
include::example$/multitenancy-ex1/src/main/resources/com/company/demo/view/user/user-detail-view.xml[tags=tenant]
----

. Добавьте следующее в класс `UserDetailView`:
+
[source,java,indent=0]
----
include::example$/multitenancy-ex1/src/main/java/com/company/demo/view/user/UserDetailView.java[tags=tenant]
----

. Чтобы разрешить использование идентичных логинов в различных тенантах, как <<tenant-users,было объяснено ранее>>, включите следующий фрагмент кода в ваш класс `LoginView`:
+
[source,java,indent=0]
----
include::example$/multitenancy-ex1/src/main/java/com/company/demo/view/login/LoginView.java[tags=tenant]
----
<1> Используйте объект `com.vaadin.flow.router.Location` для работы с текущим URL.

[[configuring-security]]
== Настройка безопасности

При настройке ролей для пользователей тенанта не включайте атрибуты `tenant-id` в политики атрибутов сущностей, чтобы скрыть их от пользователей. Например, если сущность `Customer` специфична для определенного тенанта и включает атрибут `tenant`, аннотированный `@TenantId`, роль, предоставляющая доступ к этой сущности, должна явно указывать атрибуты и исключать атрибут `tenant`:

[source,java,indent=0]
----
include::example$/multitenancy-ex1/src/main/java/com/company/demo/security/UsersRole.java[tags=mt-1]
----

[[role-assignment]]
=== Назначение ролей

Назначение ролей ограничено пользователями в рамках одного тенанта.

При назначении ролей в интерфейсе администратора система обеспечивает изоляцию тенантов следующим образом:

. В экране *Application → Users*
+
При использовании кнопки *Role assignment* в экране *Application → Users*:

* Отображаются только роли, принадлежащие тому же тенанту, что и выбранный пользователь.
* Роли из других тенантов автоматически скрываются и не отображаются.

. В экранах *Security → Resource Roles* / *Security → Row-level Roles*
+
При использовании кнопки *Assign to users* в экранах *Security → Resource roles* или *Security → Row-level roles*:

* В диалоге выбора отображаются пользователи из всех тенантов.
* Однако система не позволяет подтвердить выбор, если выбраны пользователи из разных тенантов.
* При попытке назначить роли с пересечением границ тенантов отображается предупреждающее уведомление.