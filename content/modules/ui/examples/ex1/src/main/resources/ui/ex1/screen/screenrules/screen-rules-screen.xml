<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://jmix.io/schema/ui/window"
        caption="msg://screenRulesScreen.caption">
    <layout spacing="true">
        <vbox spacing="true">
            <!-- tag::label-auto[] -->
            <label width="AUTO"
                   value="Label with content-dependent size"/>
            <!-- end::label-auto[] -->
            <label id="label" value="Label with content-dependent size"/>
            <!-- tag::size-fix[] -->
            <vbox width="320px" height="240px"/>
            <!-- end::size-fix[] -->
            <vbox id="vbox"/>
            <!-- tag::size-relative[] -->
            <label width="100%"
                   value="Label with relative size"/>
            <!-- end::size-relative[] -->
            <label id="label1"
                   value="Label with relative size"/>
        </vbox>
        <!-- tag::margin[] -->
        <vbox margin="true,false,true,false"/>
        <!-- end::margin[] -->
    </layout>
</window>