<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://jmix.io/schema/ui/window"
        caption="msg://linkButtonScreen.caption">
    <!-- tag::link-button[] -->
    <layout>
        <!-- end::link-button[] -->
        <hbox spacing="true">

            <linkButton id="saveButton1"
                        caption="Save"
                        description="Click Me!"/>

            <!-- tag::link-button[] -->
            <linkButton id="saveButton"
                        caption="Save"
                        description="Click Me!"
                        icon="SAVE"/>
            <!-- end::link-button[] -->
            <linkButton id="saveButton2"
                        description="Save"
                        icon="SAVE"/>
        </hbox>
        <!-- tag::link-button[] -->
    </layout>
    <!-- end::link-button[] -->
</window>