= SimplePagination
:page-aliases: backoffice-ui:vcl/components/simple-pagination.adoc

`SimplePagination - 简单分页` 是一个分页加载数据的组件。设计用来在 `Table`、`DataGrid` 和其他列表组件内部使用。

组件的 XML 名称：`simplePagination`。

== 基本用法

`SimplePagination` 的外观很简单，包含行数和导航按钮。与 xref:vcl/components/pagination.adoc[Pagination] 不同，`SimplePagination` 不支持特定的页数。

`SimplePagination` 也可以带每页条数的 xref:vcl/components/pagination.adoc#items-per-page[下拉列表]。

image::vcl/components/simple-pagination.png[align="center"]

Jmix Studio 在为实体创建浏览界面和主从界面时，默认生成 `SimplePagination`。

== 数据绑定

如需创建关联至数据的 `SimplePagination`，可以用 XML 的 xref:vcl/components/pagination.adoc#container-provider[containerProvider] 和 xref:vcl/components/pagination.adoc#loader-provider[loaderProvider] 内部元素。

[NOTE]
====
如果是在 `Table`、`DataGrid` 等列表组件中使用 `SimplePagination`，则无需指定数据加载器和容器的 provider，会自动从列表组件中获取。
====

下面示例将 `simplePagination` 作为 `table` 的一部分进行声明。

[source, xml,indent=0]
----
include::example$/ex1/src/main/resources/ui/ex1/screen/component/simplepagination/simplepagination-screen.xml[tags=data;layout-start;table;layout-end]
----

[[auto-load]]
== 自动加载

`SimplePagination` 显示当前数据的行数以及一个问号图标 image:vcl/components/simple-pagination-question-mark.png[align="center"]。如果点击问号图标，该组件会加载数据的总行数。

`autoLoad` 属性设定是否自动加载总行数。当 `autoLoad = "false"` 时，组件不知道数据的总行数，显示 *[?]* 按钮。当 `autoLoad = "true"` 时，组件会执行一个查询语句获取数据的总数并展示。默认为 `false`。

[[handlers]]
== 事件和处理器

include::../events-hanlers-generation-tip.adoc[]

[[after-refresh-event]]
=== AfterRefreshEvent

参阅 xref:vcl/components/pagination.adoc#after-refresh-event[AfterRefreshEvent]。

[[before-refresh-event]]
=== BeforeRefreshEvent

参阅 xref:vcl/components/pagination.adoc#before-refresh-event[BeforeRefreshEvent]。

[[total-count-delegate]]
=== TotalCountDelegate

参阅 xref:vcl/components/pagination.adoc#total-count-delegate[TotalCountDelegate]。

[[xml]]
== XML 属性

include::../xml-studio-tip.adoc[]

xref:vcl/xml.adoc#align[align] -
<<auto-load,autoLoad>> -
xref:vcl/xml.adoc#colspan[colspan] -
xref:vcl/xml.adoc#css[css] -
xref:vcl/xml.adoc#box-expand-ratio[box.expandRatio] -
xref:vcl/xml.adoc#height[height] -
xref:vcl/components/pagination.adoc#items-per-page-default-value[itemsPerPageDefaultValue] -
xref:vcl/components/pagination.adoc#items-per-page-options[itemsPerPageOptions] -
xref:vcl/components/pagination.adoc#items-per-page-unlimited-option-visible[itemsPerPageUnlimitedOptionVisible] -
xref:vcl/components/pagination.adoc#items-per-page-visible[itemsPerPageVisible] -
xref:vcl/xml.adoc#rowspan[rowspan] -
xref:vcl/xml.adoc#stylename[stylename] -
xref:vcl/xml.adoc#visible[visible] -
xref:vcl/xml.adoc#width[width]
