= 界面 XML 描述
:page-aliases: backoffice-ui:screens/descriptors.adoc

界面描述是一个 XML 文件，包含 xref:ui:vcl.adoc[可视化组件]、xref:ui:data-components.adoc[数据组件] 和一些界面参数的声明性定义。

TIP: 使用 xref:studio:screen-designer.adoc[Studio 界面设计器] 配置 XML 描述。

例如，这是编辑界面的 XML 描述：

[source,xml,indent=0]
----
include::example$/ex1/src/main/resources/ui/ex1/screen/screens/sample-screen.xml[tags=sample]
----

`window` 元素是界面 XML 描述的根元素，有如下属性：

* `messagesGroup` - 与该窗口关联的任意 xref:localization:message-bundles.adoc#group-and-key[消息组]。如果 `messagesGroup` 未定义，则加载关联至该界面类的消息包中的消息。
* `caption` - 窗口 xref:ui:vcl/xml.adoc#caption[标题]，可以包含上面消息组中的一个消息键值。
* `description` - 窗口的相关其他信息，可以可以包含上面消息组中的一个消息键值。
* `icon` - 窗口 xref:vcl/xml.adoc#icon[图标]。
* `extend` - 用于 xref:modularity:extension.adoc#ui[扩展界面]。
* `focusComponent` - 一个组件的 id，界面展示时，默认焦点位于该组件。

界面描述的元素：

* `data` - 定义界面的 xref:ui:data-components.adoc[数据组件]。
* `facets` - 定义界面的 xref:ui:facets.adoc[facets] 列表。
* `actions` - 定义界面的 xref:ui:actions.adoc[操作] 列表。
* `dialogMode` - 定义界面作为对话框打开时的大小及行为的设置。
+
`dialogMode` 的属性：
+
** `closeable` - 定义对话框窗口是否有关闭按钮。可选值：`true`、`false`。
** `closeOnClickOutside` - 当窗口是模态窗时，定义是否允许通过单击窗口之外的区域来关闭对话框窗口。可选值：`true`、`false`。
** `forceDialog` - 指定界面应始终以对话框方式打开，可选值：`true`、`false`。
** `height` - 设置对话框窗口的高度。
** `modal` - 指定对话框窗口是否是模态框。可选值：`true`、`false`。
** `positionX` - 设置对话框窗口左上角的 `x` 坐标。
** `positionY` - 设置对话框窗口左上角的 `y` 坐标。
** `resizable` - 定义用户是否可以更改对话窗口的大小。可选值：`true`、`false`。
** `width` - 设置对话框窗口的宽度。
+
[source,xml,indent=0]
----
include::example$/ex1/src/main/resources/ui/ex1/screen/screens/order-edit1.xml[tags=dialog-mode]
----

* `layout` - 界面 xref:ui:vcl/containers/layout.adoc[布局] 的根元素。
