= GroupDataGrid 组件

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/group-data-grid" class="live-demo-btn" target="_blank">在线示例</a>
</div>
++++

`groupDataGrid` 组件是比基本的表格更强大的版本，能够进行行内分组。可以根据重复的列值将行分组成为可折叠的分组块，支持多层分组和拖动重排控制。

[cols="1h,6", grid=none]
|===
|XML 元素
|`groupDataGrid`
|Java 类
|`GroupDataGrid`
|XML 属性
|*通用：* xref:flow-ui:vc/common-attributes.adoc#id[id] - <<aggregatable,aggregatable>> - <<columnReorderingAllowed,columnReorderingAllowed>> - <<emptyStateText,emptyStateText>> - xref:flow-ui:vc/common-attributes.adoc#enabled[enabled] - <<selectionMode,selectionMode>> - xref:flow-ui:vc/common-attributes.adoc#visible[visible]
|
|*数据绑定：* xref:flow-ui:vc/common-attributes.adoc#dataContainer[dataContainer] - xref:flow-ui:vc/common-attributes.adoc#metaClass[metaClass]
|
|*尺寸：* xref:flow-ui:vc/common-attributes.adoc#height[height] - xref:flow-ui:vc/common-attributes.adoc#maxHeight[maxHeight] - xref:flow-ui:vc/common-attributes.adoc#maxWidth[maxWidth] - xref:flow-ui:vc/common-attributes.adoc#minHeight[minHeight] - xref:flow-ui:vc/common-attributes.adoc#minWidth[minWidth] - xref:flow-ui:vc/common-attributes.adoc#width[width]
|
|*位置：* xref:flow-ui:vc/common-attributes.adoc#alignSelf[alignSelf] - xref:flow-ui:vc/common-attributes.adoc#colspan[colspan]
|
|*外观：* xref:flow-ui:vc/common-attributes.adoc#classNames[classNames] - xref:flow-ui:vc/common-attributes.adoc#css[css] - xref:flow-ui:vc/common-attributes.adoc#themeNames[themeNames]
|
|*其他：* <<aggregationPosition,aggregationPosition>> - <<allRowsVisible,allRowsVisible>> - xref:flow-ui:vc/common-attributes.adoc#ariaLabel[ariaLabel] - <<columnRendering,columnRendering>> - <<detailsVisibleOnClick,detailsVisibleOnClick>> - <<dropMode,dropMode>> - <<editorBuffered,editorBuffered>> - xref:flow-ui:vc/common-attributes.adoc#focusShortcut[focusShortcut] - <<multiSort,multiSort>> - <<multiSortOnShiftClickOnly,multiSortOnShiftClickOnly>> - <<multiSortPriority,multiSortPriority>> - <<nestedNullBehavior,nestedNullBehavior>> - <<pageSize,pageSize>> - <<rowsDraggable,rowsDraggable>> - <<sortByGroupEnabled,sortByGroupEnabled>> - xref:flow-ui:vc/common-attributes.adoc#tabIndex[tabIndex]
|事件和处理器
|xref:flow-ui:vc/common-handlers.adoc#AttachEvent[AttachEvent] - xref:flow-ui:vc/common-handlers.adoc#BlurEvent[BlurEvent] - <<CellFocusEvent,CellFocusEvent>> - <<CollapseEvent,CollapseEvent>> - <<ColumnReorderEvent,ColumnReorderEvent>> - <<ColumnResizeEvent,ColumnResizeEvent>> - xref:flow-ui:vc/common-handlers.adoc#DetachEvent[DetachEvent] - <<ExpandEvent,ExpandEvent>> - xref:flow-ui:vc/common-handlers.adoc#FocusEvent[FocusEvent] - <<GridDragEndEvent,GridDragEndEvent>> - <<GridDragStartEvent,GridDragStartEvent>> - <<GridDropEvent,GridDropEvent>> - <<GroupItemClickEvent,GroupItemClickEvent>> - <<GroupItemDoubleClickEvent,GroupItemDoubleClickEvent>> - <<GroupingChangedEvent,GroupingChangedEvent>> - <<ItemClickEvent,ItemClickEvent>> - <<ItemDoubleClickEvent,ItemDoubleClickEvent>> - <<SelectionEvent,SelectionEvent>> - <<SortEvent,SortEvent>> - <<dataGenerator,dataGenerator>> - <<dragFilter,dragFilter>> - <<dropFilter,dropFilter>> - <<enterPressHandler,enterPressHandler>> - <<groupPartNameGenerator,groupPartNameGenerator>> - <<groupTooltipGenerator, groupTooltipGenerator>> - <<itemSelectableProvider,itemSelectableProvider>> - <<partNameGenerator,partNameGenerator>> - <<tooltipGenerator,tooltipGenerator>>
|XML 内部元素
|<<groupBy, groupBy>> - <<columnRef,columnRef>> - <<columns,columns>> - <<column,column>> - <<groupColumn,groupColumn>> - xref:flow-ui:vc/components/dataGrid.adoc#actions-column[editorActionsColumn]
|===

[[Basics]]
== 基本用法

首先，请 xref:groupdatagrid:index.adoc#installation[安装扩展组件]，然后通过 Studio xref:studio:view-designer.adoc[视图设计器] 的 *Add Component* 工具箱添加该组件。

image::groupdatagrid-palette.png[align="center",width="765"]

下面的例子中，用户按 *Grade* 列进行了分组：

image::groupdatagrid-basic.png[align="center",width="1097"]

.XML 代码
[%collapsible%open]
====
[source,xml,indent=0]
----
include::example$/groupdatagrid-ex1/src/main/resources/com/company/groupdatagridex1/view/basicgroupgrid/basic-group-grid-view.xml[tags=view;data;facets;layout;customersDc;basics]
----
<1> 如果组件是手动添加而不是通过 *Add Component* 工具箱添加，则需要声明 `groupg` 命名空间。
<2> 表格初始化是通过 `grade` 属性分组；也可以添加更多的分组配置。
<3> 添加分组列。
====

[[data-binding]]
== 数据绑定

数据绑定是指将组件与数据容器进行关联。将组件绑定数据时，使用 `dataContainer` 属性并引用 xref:flow-ui:data/collection-container.adoc[集合数据容器]。请参考上面的示例。

[[group-item]]
== 分组项

分组项是由分组产生的特殊记录。以行的形式出现，仅包含分组表头。

image::groupdatagrid-group-items.png[align="center",width="1097"]

为了渲染分组项，数据绑定层会创建一个临时的空实体或 DTO 实例。该实例不会添加到 xref:flow-ui:data/collection-container.adoc[集合容器] 中，仅在分组时存在于内存中。

[[hide-group-item-count]]
=== 隐藏分组项计数

默认情况下，分组项显示分组内条目的数量。使用 <<groupColumn,groupColumn>> 的 `displayItemCount` 属性可以隐藏该数字。

image::groupdatagrid-hide-count.png[align="center",width="1097"]

[[multi-level-grouping]]
== 多层分组

组件支持按多列分组。点击分组列的图标可以打开 *Group by* 对话框，然后可以添加或移除列。

image::groupdatagrid-multiple.png[align="center",width="1100"]

[[sorting]]
== 排序

关于排序的配置，与 xref:flow-ui:vc/components/dataGrid.adoc#sorting[dataGrid] 的配置相同。<<sortByGroupEnabled,sortByGroupEnabled>> 属性定义排序是否影响分组顺序。

[[disable-grouping]]
== 禁用分组

如需禁止用于基于特定列进行分组，可以设置该列的 `groupAllowed` 属性为 `false`。这样就不会在 *Group by* 对话框中显示该列。下面的示例中，不允许按 *Country* 列分组：

image::groupdatagrid-groupallowed.png[align="center",width="1100"]

.XML 代码
[%collapsible]
====
[source,xml,indent=0]
----
include::example$/groupdatagrid-ex1/src/main/resources/com/company/groupdatagridex1/view/disablegrouping/disable-grouping-view.xml[tags=disable-grouping]
----
====

如需禁止终端用户使用分组功能，设置 <<groupColumn>> 元素的 `displayColumnsGrouperOnIconClick="false"`。

NOTE: 默认的分组仍然可以通过 <<groupBy>> 元素声明或 `groupBy()` 方法编程式设置。

[[custom-grouping-column]]
== 自定义分组列

如需使用自定义分组值，在视图控制器中添加新的分组属性描述。以下示例引入了一个将名字和姓氏结合的新分组属性。

这种合成属性不会自动排序。为了让排序机制也能正常，可以在分组属性描述符中使用 `.withSortProperties(...)` 指定排序属性。

image::groupdatagrid-custom-column.png[align="center",width="1097"]

.XML 代码
[%collapsible]
====
[source,xml,indent=0]
----
include::example$/groupdatagrid-ex1/src/main/resources/com/company/groupdatagridex1/view/customgrouping/custom-grouping-view.xml[tags=custom-grouping]
----
====
.Java 代码
[%collapsible]
====
[source,java,indent=0]
----
include::example$/groupdatagrid-ex1/src/main/java/com/company/groupdatagridex1/view/customgrouping/CustomGroupingView.java[tags=custom-grouping;renderer]
----
<1> 描述一个新的分组属性，由 `firstName` 和 `lastName` 拼接而成。
<2> `.withSortProperties(List.of("firstName", "lastName"))` 告诉组件如何对合成属性排序。
<3> 默认按 `fullName` 分组。
<4> 为“全名”列提供一个渲染回调，可以在不分组时显示内容。否则，不分组时列显示为空。
====

[[export-to-excel]]
== 导出 Excel

导出功能由免费的 xref:grid-export:index.adoc[] 扩展组件提供，该扩展组件支持所有表格类型：`groupDataGrid`、`dataGrid` 和 `treeDataGrid`。

image::groupdatagrid-export.png[align="center",width="1090"]

该扩展组件提供三种导出选项。有不同的导出范围以及生成 Excel 的格式：

* *All Rows* – 导出所有行并展平分组。
* *Current Page* – 仅导出当前可见行，同时 *保留表格中显示的分组结构*。
* *Selected Rows* – 导出当前选中的行并展平分组。

[[Styling]]
== 样式

=== 主题

++++
<div class="jmix-ui-live-demo-container">
    <a href="https://demo.jmix.io/ui-samples/sample/data-grid-theme-variant" class="live-demo-btn" target="_blank">在线示例</a>
</div>
++++

不同的样式版本可以通过 xref:flow-ui:vc/common-attributes.adoc#themeNames[themeNames] 属性配置。多个主题可以同时使用，样式版本与 xref:flow-ui:vc/components/dataGrid.adoc[] 一样。

=== 分组标签

组件中折叠的分组组显示 `+`，展开的分组显示 `-`。如需自定义图标，可以将图标添加到应用程序样式中。把默认的 `content` 值替换为所需的图标字符代码（例如，使用 https://github.com/vaadin/web-components/blob/main/packages/icons/assets/vaadin-font-icons.json[Vaadin 字体图标^] 集合中的 `e7c1` 和 `e7bf`）：

.application.css
[source,css,indent=0]
----
vaadin-grid-tree-toggle.jmix-group-toggle {
    &::part(toggle)::before {
        content: "\e7c1";
    }

    &[expanded]::part(toggle)::before {
        content: "\e7bf";
    }
}
----

image::groupdatagrid-groupicon.png[align="center",width="1097"]

TIP: 参考 xref:flow-ui:themes/styling-ui-components.adoc#stylable-parts[组件的可样式化 parts]。

=== 分组列图标

分组列图标可通过 <<groupColumn,groupColumn>> 元素的 `groupIcon` 属性进行自定义。如需隐藏图标，将 `groupIconVisible` 设置为 `false`。

== XML 属性

xref:flow-ui:vc/common-attributes.adoc[通用属性] 对所有组件都是一样的配置。以下是与 xref:flow-ui:vc/components/dataGrid.adoc[] 共享的属性以及 `groupDataGrid` 特有的属性：

[cols="1h,5,1", grid=rows, frame=none]
|===
|*名称*
|*描述*
|*默认值*
|[[aggregatable]]aggregatable
|如果为 `true`，则启用列的聚合。参阅 xref:flow-ui:vc/components/dataGrid.adoc#aggregating[聚合]。
|`false`
|[[columnReorderingAllowed]]columnReorderingAllowed
|如果为 `true`，则允许用户更改列的顺序。
|`false`
|[[emptyStateText]]emptyStateText
|表格为空时显示的文本。使用 null 可移除当前的空状态内容。参阅 xref:flow-ui:vc/components/dataGrid.adoc#empty-state[空状态]。
|–
|[[selectionMode]]selectionMode
|设置选择模式。支持：`SINGLE`、`MULTI`。
|`SINGLE`
|[[aggregationPosition]]aggregationPosition
|如果组件中的数据是 <<aggregatable,可聚合>> 的，则决定聚合行显示在其他行的上方还是下方。支持：`TOP` 或 `BOTTOM`。参阅 xref:flow-ui:vc/components/dataGrid.adoc#aggregating[聚合]。
|`BOTTOM`
|[[allRowsVisible]]allRowsVisible
|如果为 `true`，表格将一次显示所有行且不显示滚动条，从而有效禁用虚拟滚动。即表格会同时在 DOM 中渲染所有行，而不是仅渲染可见行并在用户滚动时加载更多行。

*不建议在条目数量较大时使用此功能，因为可能会导致性能问题。*
| –
|[[columnRendering]]columnRendering
|设置列渲染模式。在 `EAGER` 模式下，所有列都会预先渲染，无论其在视窗内是否可见。在 `LAZY` 模式下，仅当相应列位于可见视窗内时才渲染主体单元格。
|`EAGER`
|[[detailsVisibleOnClick]]detailsVisibleOnClick
|如果为 `true`，则支持通过鼠标单击显示 xref:flow-ui:vc/components/dataGrid.adoc#item-details[条目详情]。
|`true`
|[[dropMode]]dropMode
|确定可以进行拖放的行。可选值：`BETWEEN`、`ON_TOP`、`ON_TOP_OR_BETWEEN`、`ON_GRID`。此特性可用于例如重新排序行以及在网格之间拖拽行。
|–
|[[editorBuffered]]editorBuffered
|如果为 `true`，则为 xref:flow-ui:vc/components/dataGrid.adoc#inline-editing[内联编辑] 启用缓存模式，即用户必须通过点击确认按钮来确认所做的更改。此模式还支持用户取消更改。在非缓存模式下，更改无需确认即可生效。
|`false`
|[[multiSort]]multiSort
|如果为 `true`，则启用按多列排序。
|`false`
|[[multiSortOnShiftClickOnly]]multiSortOnShiftClickOnly
|如果为 `true`，则仅当按住 Shift 键点击列标题时才激活多列排序。
|`false`
|[[multiSortPriority]]multiSortPriority
|设置被点击的列是添加到已排序列列表的末尾还是开头。可选值：`PREPEND`、`APPEND`。
|`PREPEND`
|[[nestedNullBehavior]]nestedNullBehavior
|设置当解析嵌套属性时（属性链中可能包含 null 值）的行为。可选值：`THROW`、`ALLOW_NULLS`。
|`THROW`
|[[pageSize]]pageSize
|设置页面大小，即一次从数据提供者获取的条目数量。
|`50`
|[[rowsDraggable]]rowsDraggable
|如果为 `true`，则允许用户在网格中拖拽行。
|`false`
|[[sortByGroupEnabled]]sortByGroupEnabled
a|如果为 `true`：

* 为分组列启用排序。
* 按其他列排序会在每个分组内重新排列行，但初始分组顺序保持不变。

如果为 `false`：

- 为分组列禁用排序。
- 按其他列排序会相应地重新排列分组顺序。
| `true`
|===

== 事件和处理器

xref:flow-ui:vc/common-handlers.adoc[通用事件和处理器] 对所有组件都是一样的配置。以下是与 xref:flow-ui:vc/components/dataGrid.adoc[] 共享的处理器以及 `groupDataGrid` 特有的处理器：

include::flow-ui:vc/handler-generation-tip.adoc[]

[cols="1h,6", grid=rows, frame=none]
|===
|*名称*
|*描述*
|[[CellFocusEvent]]CellFocusEvent
|`com.vaadin.flow.component.grid.CellFocusEvent` 当表格中的单元格获得焦点时触发。对应 `grid-cell-focus` DOM 事件。
|[[CollapseEvent]]CollapseEvent
|当分组折叠时触发。
|[[ColumnReorderEvent]]ColumnReorderEvent
|`com.vaadin.flow.component.grid.ColumnReorderEvent` 当表格中发生列重排时触发。对应 `column-reorder-all-columns` DOM 事件。
|[[ColumnResizeEvent]]ColumnResizeEvent
|`com.vaadin.flow.component.grid.ColumnResizeEvent` 当用户调整表格列的大小时触发。对应 `column-drag-resize` DOM 事件。
|[[ExpandEvent]]ExpandEvent
|当分组展开时触发。
|[[GridDragEndEvent]]GridDragEndEvent
|`com.vaadin.flow.component.grid.dnd.GridDragEndEvent` - 表格中行的拖拽结束事件。对应 `grid-dragend` DOM 事件。
|[[GridDragStartEvent]]GridDragStartEvent
|`com.vaadin.flow.component.grid.dnd.GridDragStartEvent` - 表格中行的拖拽开始事件。对应 `grid-dragstart` DOM 事件。
|[[GridDropEvent]]GridDropEvent
|`com.vaadin.flow.component.grid.dnd.GridDropEvent` - 发生在表格或其行上的放置事件。对应 `grid-drop` DOM 事件。
|[[GroupItemClickEvent]]GroupItemClickEvent
|当单击分组项内的行时触发。
|[[GroupItemDoubleClickEvent]]GroupItemDoubleClickEvent
|当双击分组项内的行时触发。
|[[GroupingChangedEvent]]GroupingChangedEvent
|当分组发生变化（添加或移除列）时触发。
|[[ItemClickEvent]]ItemClickEvent
|`com.vaadin.flow.component.grid.ItemClickEvent` 当单击表格项时触发。对应 `item-click` DOM 事件。
|[[ItemDoubleClickEvent]]ItemDoubleClickEvent
|`com.vaadin.flow.component.grid.ItemDoubleClickEvent` 当双击表格项时触发。对应 `item-double-click` DOM 事件。
|[[SelectionEvent]]SelectionEvent
|选择事件，统一了多选和单选事件（在只需要单个选中条目的情况下）。
|[[SortEvent]]SortEvent
|`com.vaadin.flow.data.event.SortEvent` - 描述 DataProvider 排序发生变化的事件。由 `SortNotifiers` 触发。
|[[dataGenerator]]dataGenerator
|为表格添加数据生成器。如果该生成器已添加，则不执行任何操作。参考 `com.vaadin.flow.data.provider.HasDataGenerators` 接口。
|[[dragFilter]]dragFilter
|允许控制哪些特定行可以被拖拽，而不是通过 <<rowsDraggable, rowsDraggable>> 使所有行都可拖拽。
|[[dropFilter]]dropFilter
|允许控制哪些特定行是有效的放置目标。
|[[enterPressHandler]]enterPressHandler
|处理用户按下 Enter 键时的事件。
|[[groupPartNameGenerator]]groupPartNameGenerator
|根据给定条件为分组项生成 CSS 类名的 parts。支持自定义分组项。
|[[groupTooltipGenerator]]groupTooltipGenerator
|根据给定条件为分组生成 tooltip。
|[[itemSelectableProvider]]itemSelectableProvider
|设置一个判断，用于检查表格中的特定项是否可由用户选择或取消选择。该方法接收一个条目实例，如果用户可以更改该条目的选择状态则返回 `true`，否则返回 `false`。
|[[partNameGenerator]]partNameGenerator
|根据给定条件为单元格生成 CSS 类名的 parts。支持根据所显示的数据自定义单元格外观。
|[[tooltipGenerator]]tooltipGenerator
|根据给定条件为列单元格生成 tooltip。参考 https://demo.jmix.io/ui-samples/sample/data-grid-tooltip-generator[在线演示^]。
|===

== XML 内部元素

[TIP]
====
如需为选中的元素添加内部元素，点击 *Jmix UI* 属性面板中的 xref:studio:screen-designer.adoc#add-column[Add] 按钮。
====

[[groupBy]]
=== groupBy

`groupBy` 元素包含一个或多个 <<columnRef,columnRef>> 元素，指定哪些列用于分组。

[[columnRef]]
=== columnRef

`columnRef` 元素定义用作分组的列。

[[columns]]
=== columns

`columns` 元素包含 `column` 元素，并且定义这些列的共享属性。

[[column]]
=== column

`column` 元素声明单独的一列。为单独的列设置属性会覆盖 `columns` 上的设置。

[[groupColumn]]
=== groupColumn

`groupColumn` 元素声明用于分组的列，以及该列相对于其他列的位置。`key` 属性为必需。
