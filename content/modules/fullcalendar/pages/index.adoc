= FullCalendar

The FullCalendar add-on provides integration with https://fullcalendar.io/[FullCalendar] JavaScript component. The add-on contains `FullCalendar` component that allows you to show events in a calendar with various views (day, week, month, etc.) and edit them by dragging and resizing.

image::overview-fullcalendar.png[align="center"]

[[installation]]
== Installation

For automatic installation through Jmix Marketplace, follow instructions in the xref:ROOT:add-ons.adoc#installation[Add-ons] section.

For manual installation, add the following dependencies to your `build.gradle`:

// todo clear version 2.4.999-SNAPSHOT
[source, groovy, indent=0]
----
include::example$/fullcalendar-ex2/build.gradle[tags=fullcalendar-dep]
----
