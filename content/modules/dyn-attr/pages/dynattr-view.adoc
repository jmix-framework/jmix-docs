= Displaying Dynamic Attributes

When you create a new detail or list view in the project with the Dynamic Attributes add-on installed, the framework provides the ability to use the <<using-dynamicattributes-facet,dynamicAttributes>> facet in XML descriptors.

Another method to show dynamic attributes in views is by employing the <<dynamic-attributes-panel,DynamicAttributesPanel>>.

[[using-dynamicattributes-facet]]
== Using dynamicAttributes Facet

The `dynamicAttributes` facet enables the display of dynamic attributes in `DataGrid` or `FormLayout` components that are associated with a data container containing entities with dynamic attributes.

=== In Views

To use dynamic attributes in a view, include the `dynamicAttributes` facet in the `facets` section of the view XML descriptor:

[source,xml,indent=0]
----
include::example$/dyn-attr-ex1/src/main/resources/com/company/demo/view/car/car-detail-view.xml[tags=dynamicAttributes]
----

After adding the facet, you can select this view in the *Category attribute details* dialog on the xref:dyn-attr:dynattr-managing.adoc#visibility[Visibility] tab.

[[dynamicAttributes]]
=== In Fragments

The `dynamicAttributes` facet can also be used in xref:flow-ui:fragments/fragments.adoc[fragments].

Add the facet to the fragment XML:

[source,xml,indent=0]
.dynamic-attributes-fragment.xml
----
include::example$/dyn-attr-ex1/src/main/resources/com/company/demo/view/dynamicattributesfragment/dynamic-attributes-fragment.xml[]
----

Fragment controller example:

.DynamicAttributesFragment.java
[source,java,indent=0]
----
include::example$/dyn-attr-ex1/src/main/java/com/company/demo/view/dynamicattributesfragment/DynamicAttributesFragment.java[tags=fragment-java]
----

Then create a view that uses this fragment.

[CAUTION]
====
A fragment using the `dynamicAttributes` facet *must have* an ID. You can set it in xref:flow-ui:fragments/facets-in-fragment.adoc#facets-with-required-id[XML or programmatically].

When configuring dynamic attribute xref:dyn-attr:dynattr-managing.adoc#visibility[visibility], the identifier for a view that uses a fragment follows this pattern: `<view_ID>.<fragment_ID>`. For example, if a view with ID `DynAttrFacetView` contains a fragment with ID `xmlFragment`, the full identifier would be `DynAttrFacetView.xmlFragment`.

image::dyn-attr-facet.png[align=centre]
====

[[dynamic-attributes-panel]]
== Using DynamicAttributesPanel

If an entity implements the `Categorized` interface, you have the option to use the `DynamicAttributesPanel` component to showcase the dynamic attributes associated with the entity. This component facilitates the selection of a category for a specific entity instance and the specification of values for the dynamic attributes within that category.

To integrate the `DynamicAttributesPanel` component into a detail view, follow these steps:

* Make sure the `category` attribute is included into the fetch plan of your categorized entity:
+
[source,xml,indent=0]
----
include::example$/dyn-attr-ex1/src/main/resources/com/company/demo/view/car/car-detail-view.xml[tags=fetchPlan]
----
+
* Add the `dynamicAttributesPanel` visual component to the view:
+
[source,xml,indent=0]
----
include::example$/dyn-attr-ex1/src/main/resources/com/company/demo/view/car/car-detail-view.xml[tags=dynamicAttributesPanel]
----

[[adding-to-business-process-forms]]
== Adding to Business Process Form

With the xref:bpm:index.adoc[] add-on, dynamic attributes can be loaded and displayed on process forms. For details and an example, see xref:bpm:bpmn/jmix-view-forms.adoc#loading-dynamic-attributes[Loading Dynamic Attributes].

// TODO commented out until https://github.com/jmix-framework/jmix/issues/965 is fixed
// == Adding Specific Attributes Explicitly
//
// Additionally, you have the option to manually include dynamic attributes on a view. To accomplish this, incorporate the <<using-dynamicattributes-facet,dynamicAttributes>> facet into the `facets` section and use the attribute’s code with `+` prefix when connecting UI components:
//
// [source,xml,indent=0]
// ----
//     <facets>
//         <dynattr:dynamicAttributes/>
//         <dataLoadCoordinator auto="true"/>
//     </facets>
//     <data>
//         <instance id="carDc"
//                   class="com.company.demo.entity.Car">
//              <fetchPlan extends="_base">
//             </fetchPlan>
//             <loader/>
//         </instance>
//     </data>
//     <layout>
//         <formLayout id="form" dataContainer="carDc">
//             <!--...-->
//             <textField property="+passengerNumberOfSeats"/>
//         </formLayout>
//         <!--...-->
//     </layout>
// ----
